# Протокол разметки статей

## Введение

Этот документ описывает стандартизированный протокол разметки для научных статей проекта Quantum Supremacy. Протокол основан на анализе статей из VP модуля проекта Palantir и адаптирован для веб-отображения.

## Базовая структура статьи

### YAML Frontmatter (обязательно)

Каждая статья должна начинаться с YAML frontmatter, содержащего метаданные:

```markdown
---
id: article-id
title: Название статьи
author: Имя Автора
date: 2025-01-01
category: physics
tags: [quantum, vacuum, energy]
description: Краткое описание статьи для превью
readingTime: 15
difficulty: intermediate
prerequisites: [quantum-basics, vacuum-intro]
---
```

**Обязательные поля:**
- `id` - уникальный идентификатор статьи (lowercase с дефисами)
- `title` - заголовок статьи
- `author` - автор статьи
- `date` - дата публикации (YYYY-MM-DD)

**Опциональные поля:**
- `category` - категория (theory, experiment, application, tutorial)
- `tags` - массив тегов
- `description` - краткое описание для превью
- `readingTime` - время чтения в минутах
- `difficulty` - сложность (beginner, intermediate, advanced, expert)
- `prerequisites` - массив ID статей-предпосылок

### Заголовок статьи

```markdown
# Название статьи

> **Краткое описание:** Одно предложение о теме статьи

---
```

### Содержание (автоматически генерируется)

```markdown
## Содержание

1. [Раздел 1](#1-раздел-1)
2. [Раздел 2](#2-раздел-2)
   - 2.1. [Подраздел](#21-подраздел)
```

**Примечание:** Якоря генерируются автоматически из заголовков или используются явно указанные ID.

## Элементы разметки

### 1. Заголовки

```markdown
# H1 - Заголовок статьи (один на статью)
## H2 - Основные разделы
### H3 - Подразделы
#### H4 - Под-подразделы
```

**Рекомендации:**
- Использовать нумерацию: `## 1. Введение`, `### 1.1. Контекст`
- Для содержания использовать якоря: `## 1. Введение {#introduction}`

### 2. Параграфы и текстовое форматирование

```markdown
Обычный текст параграфа.

**Жирный текст** для акцентов.

*Курсив* для терминов и определений.

`inline code` для переменных, формул, команд.
```

### 3. Списки

**Маркированные списки:**

```markdown
- Пункт 1
- Пункт 2
  - Подпункт 2.1
  - Подпункт 2.2
- Пункт 3
```

**Нумерованные списки:**

```markdown
1. Первый шаг
2. Второй шаг
3. Третий шаг
```

### 4. Математические формулы

#### Inline формулы (в строке)

```markdown
Энергия вакуума $E = \hbar\omega/2$ является фундаментальной.
```

**Результат:** Энергия вакуума E = ℏω/2 является фундаментальной.

#### Block формулы (отдельный блок)

```markdown
$$
F = -\frac{\pi^2 \hbar c A}{240 d^4}
$$
```

**Результат:** Формула отображается центрированно на отдельной строке.

#### Рамки для ключевых формул

```markdown
┌──────────────────────────────────────────┐
│  a = (c²/ρ_vac,0) × ∇ρ_vac               │
│                                          │
│  ГРАВИТАЦИОННОЕ = ГРАДИЕНТ ВАКУУМНОЙ    │
│  УСКОРЕНИЕ        ЭНЕРГИИ               │
└──────────────────────────────────────────┘
```

**Парсер автоматически преобразует ASCII-рамки в HTML-блоки с классом `.formula-box`.**

### 5. Специальные блоки

#### Предупреждение

```markdown
> ⚠️ **Предупреждение:**
> 
> Текст предупреждения. Может содержать несколько строк.
> Важная информация о рисках или ограничениях.
```

#### Ключевая идея/формула

```markdown
> 🔑 **Ключевая формула:**
> 
> F = GMm/r²
> 
> Закон всемирного тяготения выведен из градиента вакуума!
```

#### Заметка/пояснение

```markdown
> 💡 **Заметка:**
> 
> Полезная информация, пояснение, инсайт.
> Помогает понять концепцию глубже.
```

#### Пример

```markdown
> 📝 **Пример:**
> 
> Для Земли на поверхности:
> M = 6×10²⁴ кг, R = 6.37×10⁶ м
> a = GM/R² = 9.8 м/с² ✓
```

### 6. Таблицы

```markdown
| Величина | Формула | Примечание |
|----------|---------|------------|
| Сила Казимира | F = -π²ℏcA/(240d⁴) | d — расстояние |
| Давление | P = -π²ℏc/(240d⁴) | P < 0 → притяжение |
```

### 7. Блоки кода и диаграммы

#### Код

```markdown
```javascript
const parser = new ArticleParser();
const html = await parser.parse(markdown);
\```
```

#### Математические формулы в блоках кода

**⚠️ ВАЖНО:** Система автоматически определяет и преобразует математические формулы из блоков кода в MathJax!

Если вы поместите формулу в блок кода без указания языка, парсер автоматически:
- Определит, что это формула (по наличию математических символов, индексов, степеней)
- Преобразует специальные символы в LaTeX (₀ → _0, ² → ^2, × → \times, √ → \sqrt)
- Отобразит формулу через MathJax

**Примеры:**

```markdown
\```
R₀ = c/(H₀ × √(Ω_tot - 1))
\```

\```
E = mc²
\```

\```
H₀ ≈ 70 км/с/Мпк
\```
```

**Результат:** Все эти формулы будут автоматически преобразованы и отображены как красивые математические уравнения.

**Когда использовать блоки кода для формул:**
- Если формула содержит специальные символы (индексы, степени, греческие буквы)
- Если нужно сохранить читаемость исходного текста
- Если формула слишком сложная для inline формата

**Когда НЕ использовать блоки кода:**
- Для простых inline формул — используйте `$...$`
- Для важных уравнений — используйте `$$...$$`
- Для ASCII-диаграмм — они останутся как код

#### ASCII-диаграммы

```markdown
\```
┌─────────────────┐
│  МАССА M        │
│  Модифицирует   │
│  вакуум         │
└─────────────────┘
        ↓
┌─────────────────┐
│  ГРАДИЕНТ ∇ρ    │
└─────────────────┘
        ↓
    ГРАВИТАЦИЯ
\```
```

**Примечание:** ASCII-диаграммы (с рамками ┌─┐│└┘) автоматически определяются и НЕ преобразуются в формулы.

### 8. Изображения

#### Относительные пути (из папки статьи)

```markdown
![Диаграмма эффекта Казимира](./images/casimir-diagram.png)
```

**Парсер автоматически преобразует в:**
```html
<img src="/articles/article-name/images/casimir-diagram.png" alt="Диаграмма эффекта Казимира">
```

#### Web URL

```markdown
![External image](https://example.com/image.png)
```

**Остается без изменений.**

### 9. Ссылки

#### Внутренние ссылки (в пределах статьи)

```markdown
Как описано в [разделе 3](#3-механизм-возникновения-гравитации).
```

#### Ссылки на другие статьи

```markdown
Подробнее см. [Эффект Казимира](CASIMIR_EFFECT.md#основные-понятия).
```

**Парсер преобразует в:**
```html
<a href="/article/casimir-effect#основные-понятия" data-link>Эффект Казимира</a>
```

#### Внешние ссылки

```markdown
Источник: [Wikipedia](https://wikipedia.org)
```

### 10. Горизонтальные разделители

```markdown
---
```

Создает `<hr>` для визуального разделения разделов.

## Структура файлов статьи

### Папка статьи

```
articles/
└── article-name/
    ├── article.md           # Основной текст
    ├── metadata.json        # Метаданные
    └── images/              # Изображения статьи
        ├── diagram1.png
        └── formula-box.svg
```

### metadata.json

```json
{
  "id": "article-name",
  "title": "Полное название статьи",
  "author": "Автор",
  "date": "2025-01-01",
  "category": "theory",
  "tags": ["gravity", "quantum", "vacuum"],
  "description": "Краткое описание статьи для превью",
  "readingTime": 15,
  "difficulty": "advanced",
  "prerequisites": ["quantum-basics", "vacuum-physics"]
}
```

## Примеры полных блоков

### Пример 1: Раздел с формулой и пояснением

```markdown
## 3. Механизм возникновения гравитации

### 3.1. Изменение спектра вакуумных мод

Рассмотрим сферическую массу M радиуса R.

**Внутри массы** (r < R):

Вакуумные моды с λ > R **подавлены**, как между пластинами Казимира.

Эффективная плотность вакуумной энергии:

$$
\rho_{vac,in} = \rho_{vac,0} \times (1 - \alpha \cdot R/\lambda)
$$

где α ~ 1 — численный коэффициент.

> 💡 **Заметка:**
> 
> Это аналогично эффекту Казимира: граничные условия ограничивают
> разрешенные моды, уменьшая плотность энергии вакуума.

**Снаружи массы** (r > R):

$$
\rho_{vac}(r) = \rho_{vac,0} \times \left(1 - \beta \cdot \frac{GM}{c^2 r}\right)
$$

где β — константа, которую мы определим далее.
```

### Пример 2: Таблица с численными данными

```markdown
### 8.1. Сравнение с другими силами

| Сила | Масштаб (d=1 мкм, A=1см²) |
|------|---------------------------|
| Казимира | 10⁻¹¹ Н |
| Электростатика (1 В/м) | 10⁻⁸ Н |
| Гравитация (m=1г) | 10⁻¹⁶ Н |
| Ван-дер-Ваальс | 10⁻¹¹ - 10⁻⁹ Н |

> ⚠️ **Предупреждение:**
> 
> Эффект Казимира сравним с электростатическими силами
> на субмикронных масштабах! Критично для MEMS/NEMS устройств.
```

### Пример 3: Блок с ключевой формулой

```markdown
### 4.5. Прямая связь: ускорение ← градиент энергии вакуума

**Это ключевая формула модели!**

┌──────────────────────────────────────────────┐
│                                              │
│  a = (c²/ρ_vac,0) × ∇ρ_vac                  │
│                                              │
│  или эквивалентно:                           │
│                                              │
│  a = -(1/ρ) × ∇P_vac                        │
│                                              │
└──────────────────────────────────────────────┘

> 🔑 **Физический смысл:**
> 
> Гравитационное ускорение прямо пропорционально градиенту
> плотности вакуумной энергии!
> 
> - Коэффициент: c²/ρ_vac,0
> - Направление: от высокой плотности к низкой
> - Масса создаёт "яму" в ρ_vac → градиент → ускорение
```

## Лучшие практики

### 1. Структура

- Начинать с введения и мотивации
- Использовать иерархию заголовков (H2 → H3 → H4)
- Добавлять содержание для статей >5 разделов
- Завершать выводами и ссылками

### 2. Формулы

- Использовать inline ($...$) для простых формул в тексте
- Block ($$...$$) для важных уравнений
- **Блоки кода без языка автоматически преобразуются в MathJax** — используйте для формул со специальными символами
- Добавлять пояснения к каждой формуле
- Использовать рамки для **ключевых** формул
- Система автоматически определяет формулы по математическим символам, индексам, степеням

### 3. Специальные блоки

- ⚠️ Предупреждение — для рисков, ограничений, важных замечаний
- 🔑 Ключевая идея — для центральных концепций и формул
- 💡 Заметка — для пояснений и инсайтов
- 📝 Пример — для численных расчетов и демонстраций

### 4. Изображения

- Сохранять в папке `images/` статьи
- Использовать описательные имена файлов
- Всегда добавлять alt текст
- Оптимизировать размер (PNG для диаграмм, JPG для фото)

### 5. Ссылки

- Использовать якоря для навигации внутри статьи
- Ссылаться на другие статьи по их ID
- Внешние ссылки открывать в новой вкладке (парсер добавит target="_blank")

## Расширенные возможности

### Сноски (опционально)

```markdown
Текст со сноской[^1].

[^1]: Текст сноски внизу страницы.
```

### Определения терминов

```markdown
**Эффект Казимира**
: Квантовомеханическое явление притяжения между проводящими пластинами в вакууме.
```

### Цитаты

```markdown
> Нулевые колебания — не математическая абстракция, а физическая реальность.
> 
> — Хендрик Казимир, 1948
```

## Валидация статьи

Перед публикацией проверить:

- [ ] Заголовок H1 присутствует и уникален
- [ ] Все формулы корректно отображаются
- [ ] Изображения существуют и загружаются
- [ ] Внутренние ссылки работают
- [ ] Специальные блоки правильно отформатированы
- [ ] metadata.json заполнен
- [ ] Нет HTML-кода (только markdown)
- [ ] ASCII-рамки используют правильные символы (┌─┐└─┘│)

## Заключение

Этот протокол обеспечивает:
- **Единообразие** оформления всех статей
- **Автоматизацию** генерации содержания и якорей
- **Читаемость** исходного markdown текста
- **Гибкость** для будущих расширений

При возникновении вопросов обращайтесь к примерам из VP папки или этому документу.
