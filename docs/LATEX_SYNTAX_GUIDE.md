# 📐 LaTeX Синтаксис - Полное руководство

Подробное руководство по использованию LaTeX синтаксиса для написания математических формул в статьях проекта Quantum Supremacy.

---

## 📋 Содержание

1. [Базовые операции](#базовые-операции)
2. [Дроби и корни](#дроби-и-корни)
3. [Степени и индексы](#степени-и-индексы)
4. [Греческие буквы](#греческие-буквы)
5. [Операторы](#операторы)
6. [Символы отношений](#символы-отношений)
7. [Стрелки](#стрелки)
8. [Скобки и разделители](#скобки-и-разделители)
9. [Матрицы и системы уравнений](#матрицы-и-системы-уравнений)
10. [Специальные функции](#специальные-функции)
11. [Текст в формулах](#текст-в-формулах)
12. [Пробелы и выравнивание](#пробелы-и-выравнивание)
13. [Примеры из физики](#примеры-из-физики)
14. [Частые ошибки](#частые-ошибки)

---

## Базовые операции

### Арифметические операции

```latex
a + b          % Сложение
a - b          % Вычитание
a \times b     % Умножение (×)
a \cdot b      % Умножение (точка)
a \div b       % Деление
a / b          % Деление (простой вариант)
```

**Примеры:**
```markdown
$E = mc^2$                    % E = mc²
$F = m \times a$              % F = m × a
$v = \frac{s}{t}$             % v = s/t
```

---

## Дроби и корни

### Дроби

```latex
\frac{числитель}{знаменатель}     % Обычная дробь
\dfrac{числитель}{знаменатель}    % Большая дробь (в inline)
\tfrac{числитель}{знаменатель}    % Маленькая дробь (в block)
```

**Примеры:**
```markdown
$\frac{a}{b}$                      % a/b
$\frac{\hbar c}{d^4}$              % ℏc/d⁴
$\frac{\dot{V}}{V} = 3H$           % V̇/V = 3H
```

### Корни

```latex
\sqrt{x}                    % Квадратный корень
\sqrt[n]{x}                 % Корень n-й степени
\sqrt[3]{x}                 % Кубический корень
```

**Примеры:**
```markdown
$\sqrt{x}$                      % √x
$\sqrt[3]{8} = 2$               % ∛8 = 2
$\sqrt{\frac{a}{b}}$            % √(a/b)
```

---

## Степени и индексы

### Степени

```latex
a^b                 % Степень
a^{b+c}             % Сложная степень (скобки обязательны!)
a^{b^c}             % Вложенная степень
```

**Примеры:**
```markdown
$E = mc^2$                      % E = mc²
$10^{26}$                       % 10²⁶
$R^{3}$                         % R³
$(a+b)^2 = a^2 + 2ab + b^2$     % (a+b)² = a² + 2ab + b²
```

### Индексы

```latex
a_b                % Нижний индекс
a_{b+c}            % Сложный индекс (скобки обязательны!)
a_{b_c}            % Вложенный индекс
```

**Примеры:**
```markdown
$H_0$                          % H₀ (параметр Хаббла)
$\rho_{vac}$                   % ρ_vac
$R_{\text{obs}}$               % R_obs
$\delta g_{\mu\nu}$            % δg_μν
```

### Комбинация степеней и индексов

```latex
a_b^c              % Индекс и степень
a^c_b              % Степень и индекс (то же самое)
a_{b}^{c}          % Явное указание порядка
```

**Примеры:**
```markdown
$x_1^2 + x_2^2$                % x₁² + x₂²
$\rho_{vac}^0$                 % ρ_vac⁰
$R_0^3$                        % R₀³
```

---

## Греческие буквы

### Строчные (маленькие)

```latex
\alpha      % α
\beta       % β
\gamma      % γ
\delta      % δ
\epsilon    % ε
\varepsilon % ε (альтернативный вариант)
\zeta       % ζ
\eta        % η
\theta      % θ
\vartheta   % ϑ
\iota       % ι
\kappa      % κ
\lambda     % λ
\mu         % μ
\nu         % ν
\xi         % ξ
\pi         % π
\varpi      % ϖ
\rho        % ρ
\varrho     % ϱ
\sigma      % σ
\varsigma   % ς
\tau        % τ
\upsilon    % υ
\phi        % φ
\varphi     % φ (альтернативный вариант)
\chi        % χ
\psi        % ψ
\omega      % ω
```

### Заглавные (большие)

```latex
\Gamma      % Γ
\Delta      % Δ
\Theta      % Θ
\Lambda     % Λ
\Xi         % Ξ
\Pi         % Π
\Sigma      % Σ
\Upsilon    % Υ
\Phi        % Φ
\Psi        % Ψ
\Omega      % Ω
```

**Примеры:**
```markdown
$\alpha + \beta = \gamma$           % α + β = γ
$\lambda = \frac{h}{p}$            % λ = h/p
$\Omega_{tot} = 1$                 % Ω_tot = 1
$\Delta E = \hbar \omega$          % ΔE = ℏω
```

---

## Операторы

### Математические операторы

```latex
\sum        % Сумма (Σ)
\prod       % Произведение (Π)
\int        % Интеграл (∫)
\oint       % Контурный интеграл (∮)
\iint       % Двойной интеграл
\iiint      % Тройной интеграл
\partial    % Частная производная (∂)
\nabla      % Набла/градиент (∇)
\Delta      % Лапласиан (Δ)
\ell        % Эллиптический интеграл
```

**Примеры:**
```markdown
$\sum_{i=1}^{n} x_i$                % Σᵢ₌₁ⁿ xᵢ
$\int_0^{\infty} e^{-x} dx$        % ∫₀^∞ e⁻ˣ dx
$\frac{\partial f}{\partial x}$    % ∂f/∂x
$\nabla \cdot \vec{F}$             % ∇·F⃗
```

### Логарифмы и экспоненты

```latex
\log        % Логарифм
\ln         % Натуральный логарифм
\lg         % Десятичный логарифм
\exp        % Экспонента
```

**Примеры:**
```markdown
$\ln(x)$                            % ln(x)
$\log_{10}(x)$                      % log₁₀(x)
$e^{i\pi} = -1$                     % e^(iπ) = -1
$\exp(-\frac{E}{kT})$               % exp(-E/kT)
```

### Тригонометрические функции

```latex
\sin        % Синус
\cos        % Косинус
\tan        % Тангенс
\cot        % Котангенс
\sec        % Секанс
\csc        % Косеканс
\arcsin     % Арксинус
\arccos     % Арккосинус
\arctan     % Арктангенс
```

**Примеры:**
```markdown
$\sin(\theta)$                      % sin(θ)
$\cos(2\pi f t)$                    % cos(2πft)
$\tan^{-1}(x)$                      % tan⁻¹(x)
```

---

## Символы отношений

### Равенства и неравенства

```latex
=           % Равно
\neq        % Не равно (≠)
\equiv      % Тождественно равно (≡)
\approx     % Приблизительно равно (≈)
\simeq      % Асимптотически равно (≃)
\sim        % Пропорционально (∼)
\propto     % Пропорционально (∝)
\cong       % Конгруэнтно (≅)
```

**Примеры:**
```markdown
$a = b$                             % a = b
$E \approx mc^2$                    % E ≈ mc²
$F \propto \frac{1}{r^2}$          % F ∝ 1/r²
$\rho_{vac} \sim \frac{\hbar c}{d^4}$  % ρ_vac ~ ℏc/d⁴
```

### Сравнения

```latex
<           % Меньше
>           % Больше
\leq        % Меньше или равно (≤)
\geq        % Больше или равно (≥)
\ll         % Много меньше (≪)
\gg         % Много больше (≫)
\prec       % Предшествует (≺)
\succ       % Следует (≻)
```

**Примеры:**
```markdown
$x \leq 10$                         % x ≤ 10
$\rho_{Casimir} \ll \rho_{vac}$    % ρ_Casimir ≪ ρ_vac
$E \gg mc^2$                        % E ≫ mc²
```

---

## Стрелки

### Обычные стрелки

```latex
\rightarrow     % → (вправо)
\leftarrow      % ← (влево)
\leftrightarrow % ↔ (в обе стороны)
\Rightarrow     % ⇒ (двойная вправо)
\Leftarrow      % ⇐ (двойная влево)
\Leftrightarrow % ⇔ (двойная в обе стороны)
```

### Другие стрелки

```latex
\mapsto         % ↦ (отображается в)
\to             % → (к)
\longrightarrow % ⟶ (длинная вправо)
\longleftarrow  % ⟵ (длинная влево)
\uparrow        % ↑
\downarrow      % ↓
\updownarrow    % ↕
```

**Примеры:**
```markdown
$f: X \to Y$                    % f: X → Y
$x \mapsto f(x)$                % x ↦ f(x)
$t \to \infty$                  % t → ∞
$\lim_{x \to 0} f(x)$           % lim(x→0) f(x)
```

---

## Скобки и разделители

### Обычные скобки

```latex
( )             % Обычные скобки
[ ]             % Квадратные скобки
\{ \}           % Фигурные скобки (нужно экранировать!)
| |             % Вертикальные линии
\| \|           % Двойные вертикальные линии
```

### Автоматически масштабируемые скобки

```latex
\left( ... \right)          % Автоматический размер
\left[ ... \right]          % Квадратные
\left\{ ... \right\}        % Фигурные
\left| ... \right|          % Модуль
\left\| ... \right\|        % Норма
\left\langle ... \right\rangle  % Угловые скобки
```

**Примеры:**
```markdown
$\left(\frac{a}{b}\right)$              % (a/b) с правильным размером
$\left[\frac{\dot{V}}{V}\right]$        % [V̇/V]
$\left\{x \in \mathbb{R}\right\}$       % {x ∈ ℝ}
$\left|\vec{v}\right|$                  % |v⃗|
```

### Ручное масштабирование

```latex
\big( \big)         % Немного больше
\Big( \Big)         % Больше
\bigg( \bigg)       % Еще больше
\Bigg( \Bigg)       % Максимальный размер
```

---

## Матрицы и системы уравнений

### Матрицы

```latex
\begin{matrix}
a & b \\
c & d
\end{matrix}

\begin{pmatrix}
a & b \\
c & d
\end{pmatrix}       % Круглые скобки

\begin{bmatrix}
a & b \\
c & d
\end{bmatrix}       % Квадратные скобки

\begin{vmatrix}
a & b \\
c & d
\end{vmatrix}       % Определитель

\begin{Vmatrix}
a & b \\
c & d
\end{Vmatrix}       % Двойные линии
```

**Пример:**
```markdown
$$
\begin{pmatrix}
a & b \\
c & d
\end{pmatrix}
\begin{pmatrix}
x \\
y
\end{pmatrix}
=
\begin{pmatrix}
ax + by \\
cx + dy
\end{pmatrix}
$$
```

### Системы уравнений

```latex
\begin{cases}
x + y = 1 \\
x - y = 0
\end{cases}
```

**Пример:**
```markdown
$$
\begin{cases}
\dot{R} = HR \\
\dot{H} = -H^2 - \frac{4\pi G}{3}\rho
\end{cases}
$$
```

### Выравнивание уравнений

```latex
\begin{aligned}
x &= a + b \\
y &= c + d \\
z &= e + f
\end{aligned}
```

**Пример:**
```markdown
$$
\begin{aligned}
H(t) &= H_0 \times \frac{t_{rip} - t_0}{t_{rip} - t} \\
\text{div}(g) &= 6H(t) \to \infty \text{ при } t \to t_{rip}
\end{aligned}
$$
```

---

## Специальные функции

### Множества

```latex
\in          % Принадлежит (∈)
\notin       % Не принадлежит (∉)
\subset      % Подмножество (⊂)
\subseteq    % Подмножество или равно (⊆)
\supset      % Надмножество (⊃)
\supseteq    % Надмножество или равно (⊇)
\cap         % Пересечение (∩)
\cup         % Объединение (∪)
\emptyset    % Пустое множество (∅)
```

**Примеры:**
```markdown
$x \in \mathbb{R}$                  % x ∈ ℝ
$A \subset B$                       % A ⊂ B
$A \cap B$                          % A ∩ B
```

### Числовые множества

```latex
\mathbb{R}      % ℝ (вещественные числа)
\mathbb{N}      % ℕ (натуральные числа)
\mathbb{Z}      % ℤ (целые числа)
\mathbb{Q}      % ℚ (рациональные числа)
\mathbb{C}      % ℂ (комплексные числа)
```

**Примеры:**
```markdown
$x \in \mathbb{R}$                  % x ∈ ℝ
$n \in \mathbb{N}$                  % n ∈ ℕ
$z = a + bi, z \in \mathbb{C}$      % z = a + bi, z ∈ ℂ
```

### Другие специальные символы

```latex
\infty         % Бесконечность (∞)
\emptyset      % Пустое множество (∅)
\exists        % Существует (∃)
\forall        % Для всех (∀)
\neg           % Отрицание (¬)
\land          % И (∧)
\lor           % ИЛИ (∨)
\oplus         % Прямая сумма (⊕)
\otimes        % Тензорное произведение (⊗)
\perp          % Перпендикулярно (⊥)
\parallel      % Параллельно (∥)
\angle         % Угол (∠)
\measuredangle % Измеренный угол (∡)
\sphericalangle % Сферический угол (∢)
```

**Примеры:**
```markdown
$\lim_{x \to \infty} f(x)$         % lim(x→∞) f(x)
$\forall \epsilon > 0$             % ∀ε > 0
$\vec{a} \perp \vec{b}$            % a⃗ ⊥ b⃗
```

---

## Текст в формулах

### Обычный текст

```latex
\text{текст}           % Обычный текст в формуле
\textrm{текст}         % Римский шрифт
\textbf{текст}         % Жирный текст
\textit{текст}         % Курсив
```

**Примеры:**
```markdown
$\text{div}(g) = 6H$               % div(g) = 6H
$\rho_{\text{vac}}$                % ρ_vac
$\text{при } t \to \infty$        % при t → ∞
$R \sim 10^{26} \text{ м}$         % R ~ 10²⁶ м
```

### Шрифты

```latex
\mathrm{ABC}       % Римский (ABC)
\mathbf{ABC}       % Жирный (ABC)
\mathit{ABC}       % Курсив (ABC)
\mathsf{ABC}       % Sans-serif (ABC)
\mathtt{ABC}       % Моноширинный (ABC)
\mathcal{ABC}      % Каллиграфический (𝒜ℬ𝒞)
\mathbb{ABC}       % Двойной штрих (𝔸𝔹ℂ)
\mathfrak{ABC}     % Фрактурный (𝔄𝔅ℭ)
```

---

## Пробелы и выравнивание

### Пробелы

```latex
\,          % Тонкий пробел
\;          % Средний пробел
\:          % Средний пробел (альтернатива)
\           % Обычный пробел
\quad       % Большой пробел (1em)
\qquad      % Очень большой пробел (2em)
```

**Примеры:**
```markdown
$x \, dx$                       % x dx (с тонким пробелом)
$a \quad b$                     % a    b (с большим пробелом)
$\text{при } t \to \infty$     % при t → ∞ (с пробелом)
```

### Выравнивание в многострочных формулах

```markdown
$$
\begin{aligned}
x &= a + b + c \\
  &= d + e + f \\
  &= g + h + i
\end{aligned}
$$
```

---

## Примеры из физики

### Квантовая механика

```markdown
Уравнение Шрёдингера:

$$i\hbar \frac{\partial \Psi}{\partial t} = \hat{H} \Psi$$

Энергия фотона: $E = h\nu = \hbar \omega$

Принцип неопределенности: $\Delta x \cdot \Delta p \geq \frac{\hbar}{2}$
```

### Общая теория относительности

```markdown
Уравнение Эйнштейна:

$$G_{\mu\nu} = \frac{8\pi G}{c^4} T_{\mu\nu}$$

Метрика Шварцшильда:

$$ds^2 = -\left(1 - \frac{2GM}{c^2 r}\right) dt^2 + \left(1 - \frac{2GM}{c^2 r}\right)^{-1} dr^2 + r^2 d\Omega^2$$
```

### Космология

```markdown
Параметр Хаббла: $H_0 \approx 70 \text{ км/с/Мпк}$

Критическая плотность: $\rho_{crit} = \frac{3H^2}{8\pi G}$

Уравнение Фридмана:

$$\left(\frac{\dot{R}}{R}\right)^2 = \frac{8\pi G}{3}\rho - \frac{k c^2}{R^2}$$

Дивергенция метрики: $\text{div}(g) = 6H(t)$
```

### Эффект Казимира

```markdown
Сила Казимира:

$$F = -\frac{\pi^2 \hbar c A}{240 d^4}$$

Плотность энергии:

$$\rho_{\text{Casimir}} \sim \frac{\hbar c}{d^4}$$

Для космологических масштабов:

$$\rho_{\text{Casimir}} \sim \frac{\hbar c}{R^4} \sim 10^{-138} \text{ Дж/м}^3 \ll \rho_{\text{vac}}$$
```

---

## Частые ошибки

### ❌ Неправильно

```markdown
$E=mc^2$                    % Нет пробелов
$\frac ab$                  % Нет фигурных скобок
$a^bc$                      % Неправильный порядок (нужно a^{bc})
$\text{div}(g)=6H$          % Нет пробелов вокруг знака =
$\left(\frac{a}{b}$         % Не закрыта скобка
```

### ✅ Правильно

```markdown
$E = mc^2$                  % С пробелами
$\frac{a}{b}$               % С фигурными скобками
$a^{bc}$                    % Правильный порядок
$\text{div}(g) = 6H$        % С пробелами
$\left(\frac{a}{b}\right)$  % Закрыта скобка
```

### Правила использования скобок

1. **Всегда используйте фигурные скобки** для сложных выражений:
   - `$a^{b+c}$` ✅ (правильно)
   - `$a^b+c$` ❌ (неправильно, будет aᵇ+c)

2. **Используйте `\left` и `\right`** для автоматического масштабирования:
   - `$\left(\frac{a}{b}\right)$` ✅
   - `$(\frac{a}{b})$` ❌ (скобки будут слишком маленькими)

3. **Экранируйте фигурные скобки** в обычном тексте:
   - `$\{x, y, z\}$` ✅
   - `${x, y, z}$` ❌ (ошибка)

### Пробелы в формулах

- **Всегда ставьте пробелы** вокруг операторов: `$a = b$`, `$x + y$`
- **Используйте `\,`** для тонких пробелов: `$x \, dx$`
- **Используйте `\text{}`** для текста: `$\text{при } t \to \infty$`

---

## Быстрая справка

### Самые частые команды

| Команда | Результат | Пример |
|---------|-----------|--------|
| `\frac{a}{b}` | Дробь | `$\frac{1}{2}$` → ½ |
| `a^b` | Степень | `$x^2$` → x² |
| `a_b` | Индекс | `$H_0$` → H₀ |
| `\sqrt{x}` | Корень | `$\sqrt{2}$` → √2 |
| `\alpha` | α | `$\alpha$` → α |
| `\pi` | π | `$\pi$` → π |
| `\infty` | ∞ | `$\infty$` → ∞ |
| `\sum` | Σ | `$\sum$` → Σ |
| `\int` | ∫ | `$\int$` → ∫ |
| `\partial` | ∂ | `$\partial$` → ∂ |
| `\nabla` | ∇ | `$\nabla$` → ∇ |
| `\approx` | ≈ | `$\approx$` → ≈ |
| `\leq` | ≤ | `$\leq$` → ≤ |
| `\geq` | ≥ | `$\geq$` → ≥ |
| `\neq` | ≠ | `$\neq$` → ≠ |
| `\text{текст}` | Текст | `$\text{div}$` → div |

---

## Полезные ресурсы

### Онлайн редакторы LaTeX

- [Overleaf](https://www.overleaf.com/) - Онлайн LaTeX редактор
- [Detexify](http://detexify.kirelabs.org/classify.html) - Рисуй символ, получай LaTeX код
- [LaTeX Symbol List](https://www.rpi.edu/dept/arc/training/latex/LaTeX_symbols.pdf) - Полный список символов

### Справочники

- [LaTeX/Mathematics](https://en.wikibooks.org/wiki/LaTeX/Mathematics) - Викиучебник по LaTeX
- [MathJax Documentation](https://www.mathjax.org/) - Документация MathJax
- [LaTeX Cheat Sheet](https://wch.github.io/latexsheet/) - Шпаргалка по LaTeX

---

## Заключение

Это руководство покрывает основные аспекты LaTeX синтаксиса для написания математических формул. Помните:

1. **Используйте фигурные скобки** для сложных выражений
2. **Ставьте пробелы** вокруг операторов
3. **Используйте `\left` и `\right`** для автоматического масштабирования скобок
4. **Используйте `\text{}`** для обычного текста в формулах
5. **Проверяйте результат** в браузере после написания

Удачи в написании формул! 🚀

---

**Последнее обновление:** 2025-01-27
