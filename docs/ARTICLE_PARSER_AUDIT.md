# 🔍 Аудит модуля Article Parser - Защита формул от marked.js

## Проблема

marked.js может удалять или экранировать HTML теги и комментарии, что приводит к потере плейсхолдеров для математических формул.

## Анализ текущей реализации

### Текущий метод защиты формул

1. **Метод защиты**: HTML теги с data-атрибутами
   - `<div data-math-placeholder="BLOCK_X">` для block формул
   - `<span data-math-placeholder="INLINE_X">` для inline формул
   - Содержат невидимые маркеры (zero-width spaces) для предотвращения удаления пустых тегов

2. **Настройки marked.js**:
   ```javascript
   marked.setOptions({
       sanitize: false,  // НЕ экранируем HTML
       breaks: true,
       gfm: true,
       headerIds: true,
       mangle: false
   });
   ```

### Потенциальные проблемы

1. **marked.js может экранировать HTML теги** в некоторых контекстах
   - Даже с `sanitize: false`, marked.js может обрабатывать HTML теги как часть markdown разметки
   - HTML теги внутри параграфов могут быть экранированы

2. **Пустые теги могут удаляться**
   - Даже с невидимым содержимым, некоторые парсеры могут удалять теги

3. **Обработка в параграфах**
   - marked.js оборачивает текст в `<p>` теги, что может нарушить структуру block формул

## Реализованные исправления

### 1. Улучшенные плейсхолдеры

```javascript
// Используем теги с содержимым (невидимым маркером)
const marker = `\u200B${type}_${index}\u200B`;
// Для block формул - div с переносами строк
return `\n<div data-math-placeholder="${type}_${index}" style="display:none;">${marker}</div>\n`;
// Для inline формул - span
return `<span data-math-placeholder="${type}_${index}" style="display:none;">${marker}</span>`;
```

### 2. Кастомный рендерер для параграфов

```javascript
renderer.paragraph = (text) => {
    // Если параграф содержит плейсхолдеры, не оборачиваем в <p>
    if (text.includes('data-math-placeholder')) {
        return text + '\n';
    }
    return originalParagraph ? originalParagraph(text) : `<p>${text}</p>\n`;
};
```

### 3. Проверка сохранения плейсхолдеров

```javascript
// После парсинга проверяем, сохранились ли плейсхолдеры
const placeholderCount = (html.match(/data-math-placeholder="[^"]*"/g) || []).length;
const expectedCount = (markdownText.match(/data-math-placeholder="[^"]*"/g) || []).length;
if (placeholderCount < expectedCount) {
    console.error(`❌ marked.js удалил плейсхолдеры!`);
}
```

### 4. Улучшенный regex для восстановления

```javascript
// Многострочный regex, который учитывает возможные пробелы и переносы строк
const placeholderRegex = new RegExp(
    `<${tagName}\\s+data-math-placeholder="${placeholderType}_${index}"[^>]*>[\\s\\S]*?${escapedMarker}[\\s\\S]*?</${tagName}>`,
    'g'
);
```

## Альтернативные методы (если текущий не работает)

### Вариант 1: Использование блоков кода

```javascript
// Заменяем формулы на блоки кода с специальным языком
const placeholder = `\`\`\`math-formula-${type}-${index}\n${formula}\n\`\`\``;
// Затем в кастомном рендерере кода восстанавливаем формулы
```

### Вариант 2: Использование специальных Unicode символов

```javascript
// Используем специальные Unicode символы, которые точно не будут удалены
const PLACEHOLDER_START = '\uE000'; // Private Use Area
const PLACEHOLDER_END = '\uE001';
const placeholder = `${PLACEHOLDER_START}MATH_${type}_${index}${PLACEHOLDER_END}`;
```

### Вариант 3: Прямая обработка формул в marked.js

```javascript
// Используем кастомный токенизатор для обработки формул напрямую
marked.use({
    extensions: [{
        name: 'mathFormula',
        level: 'block',
        tokenizer(src) {
            const match = src.match(/^\$\$([\s\S]*?)\$\$/);
            if (match) {
                return {
                    type: 'mathFormula',
                    raw: match[0],
                    formula: match[1].trim()
                };
            }
        },
        renderer(token) {
            return `$$${token.formula}$$`;
        }
    }]
});
```

## Рекомендации

1. **Мониторинг**: Всегда проверяйте количество плейсхолдеров до и после парсинга
2. **Логирование**: Включите `window.DEBUG_ARTICLE_PARSER = true` для детального логирования
3. **Тестирование**: Тестируйте с разными версиями marked.js
4. **Fallback**: Если плейсхолдеры теряются, используйте альтернативный метод

## Текущий статус

✅ Исправления внесены:
- Улучшены плейсхолдеры (теги с содержимым)
- Добавлен кастомный рендерер для параграфов
- Добавлена проверка сохранения плейсхолдеров
- Улучшен regex для восстановления формул

⚠️ Требуется тестирование:
- Проверить, сохраняются ли плейсхолдеры после парсинга
- Проверить, правильно ли восстанавливаются формулы
- Проверить работу с разными версиями marked.js

## Следующие шаги

1. Протестировать текущую реализацию
2. Если плейсхолдеры все еще теряются, перейти на альтернативный метод
3. Рассмотреть возможность использования markdown-it вместо marked.js (более гибкий)
