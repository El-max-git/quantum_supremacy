# Руководство по Article Parser

## Введение

Article Parser — это система для преобразования markdown статей в HTML с поддержкой математических формул, специальных блоков, изображений и внутренних ссылок. Система интегрирована с SPA роутером проекта Quantum Supremacy.

## Архитектура системы

### Компоненты

```
Article Parser System
├── js/article-parser.js       - Основной парсер
├── js/table-of-contents.js    - Генератор содержания
├── pages/article-template.html - Шаблон страницы
├── css/style.css (+ article styles) - Стили
└── data/config.json (articles) - Конфигурация
```

### Зависимости

- **marked.js** (v11.1.1) - парсинг markdown → HTML
- **MathJax 3.x** - рендеринг математических формул
- **Browser APIs** - DOM manipulation, fetch, History API

## Использование

### 1. Создание новой статьи

#### Шаг 1: Создать структуру папок

```bash
articles/
└── my-article/
    ├── article.md          # Основной текст
    ├── metadata.json       # Метаданные
    └── images/             # Изображения (опционально)
```

#### Шаг 2: Написать markdown файл

Создать `articles/my-article/article.md` согласно [протоколу разметки](ARTICLE_PROTOCOL.md).

#### Шаг 3: Создать metadata.json

```json
{
  "id": "my-article",
  "title": "Заголовок статьи",
  "author": "Имя автора",
  "date": "2025-01-01",
  "category": "theory",
  "tags": ["quantum", "physics"],
  "description": "Краткое описание",
  "readingTime": 10,
  "difficulty": "intermediate"
}
```

#### Шаг 4: Зарегистрировать в config.json

Добавить в `data/config.json`:

```json
{
  "articles": [
    {
      "id": "my-article",
      "title": "Заголовок статьи",
      "path": "/article/my-article",
      "mdFile": "articles/my-article/article.md",
      "category": "theory",
      "author": "Имя автора",
      "date": "2025-01-01",
      "readingTime": 10,
      "tags": ["quantum", "physics"],
      "description": "Краткое описание"
    }
  ]
}
```

### 2. Программный API

#### ArticleParser

```javascript
// Инициализация парсера
const parser = new ArticleParser({
    basePath: '/articles',
    mathRenderer: 'mathjax',
    autoAnchors: true
});

// Парсинг markdown
const html = await parser.parse(markdownText, articlePath);

// Инициализация MathJax (глобально)
ArticleParser.initMathJax();

// Рендеринг математики в элементе
await ArticleParser.renderMath(element);
```

#### TableOfContents

```javascript
// Создание генератора
const toc = new TableOfContents({
    minHeadings: 3,
    maxLevel: 4,
    scrollOffset: 80
});

// Генерация содержания
const tocHtml = toc.generate(contentElement);

// Инициализация отслеживания скролла
toc.initScrollTracking();

// Быстрая генерация (статический метод)
const html = TableOfContents.quick(contentElement);
```

### 3. Интеграция с роутером

Статьи автоматически загружаются через template `pages/article-template.html`. Роутер определяет articleId из URL и загружает соответствующую статью.

**URL формат:**
```
/article/{article-id}
```

**Пример:**
```
https://example.com/article/gravity-casimir
→ загружает articles/gravity-casimir/article.md
```

## Конфигурация

### ArticleParser options

| Параметр | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| `basePath` | string | `'/articles'` | Базовый путь для статей |
| `mathRenderer` | string | `'mathjax'` | Рендерер математики |
| `autoAnchors` | boolean | `true` | Автогенерация якорей |
| `imageBasePath` | string | `''` | Базовый путь для изображений |

### TableOfContents options

| Параметр | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| `minHeadings` | number | `3` | Минимум заголовков для TOC |
| `maxLevel` | number | `4` | Максимальный уровень (H1-H4) |
| `scrollOffset` | number | `80` | Отступ при скролле (px) |
| `activeClass` | string | `'active'` | Класс для активной ссылки |

## Специальные возможности

### Предобработка

Парсер выполняет несколько проходов предобработки:

1. **Специальные блоки** - преобразование `> ⚠️ **...:**` в HTML блоки
2. **Рамки формул** - преобразование ASCII-рамок в `.formula-box`
3. **Markdown → HTML** - стандартный парсинг через marked.js
4. **Изображения** - преобразование относительных путей
5. **Ссылки** - обработка внутренних ссылок для SPA
6. **Якоря** - автогенерация ID для заголовков

### Типы специальных блоков

```markdown
> ⚠️ **Предупреждение:** - .article-block.warning
> 🔑 **Ключевая идея:** - .article-block.key-idea
> 💡 **Заметка:** - .article-block.note
> 📝 **Пример:** - .article-block.example
```

### Обработка изображений

- `./images/pic.png` → `/articles/{id}/images/pic.png`
- `https://...` → без изменений
- `pic.png` → `/articles/{id}/images/pic.png`

### Обработка ссылок

- `#anchor` → без изменений (якорь в текущей статье)
- `OTHER.md#section` → `/article/other#section` + `data-link`
- `https://...` → `target="_blank" rel="noopener noreferrer"`

## Стилизация

### CSS классы

**Layout:**
- `.article-wrapper` - основной контейнер (grid layout)
- `.article-toc-sidebar` - боковая панель содержания
- `.article-content` - контейнер контента

**Content:**
- `.article-header` - заголовок статьи
- `.article-body` - тело статьи
- `.article-footer` - подвал (навигация)

**Special blocks:**
- `.article-block` - базовый класс блока
- `.article-block.warning` - предупреждение
- `.article-block.key-idea` - ключевая идея
- `.article-block.note` - заметка
- `.article-block.example` - пример

**Formula:**
- `.formula-box` - рамка для формул
- `.formula-box-content` - содержимое рамки

**TOC:**
- `.toc-nav` - навигация содержания
- `.toc-list` - список заголовков
- `.toc-link` - ссылка на раздел
- `.toc-link.active` - активная ссылка

### Темы (dark/light)

Все стили поддерживают обе темы через CSS переменные и `@media (prefers-color-scheme: dark)`.

## Расширение функциональности

### Добавление нового типа блока

1. Добавить паттерн в `ArticleParser.preprocessSpecialBlocks()`:

```javascript
newType: {
    regex: /^>\s*🎯\s*\*\*([^:]+):\*\*\s*\n((?:^>.*\n?)+)/gm,
    type: 'new-type',
    icon: '🎯'
}
```

2. Добавить стили в `css/style.css`:

```css
.article-block.new-type {
    background: #f0f9ff;
    border-color: #0ea5e9;
    color: #0c4a6e;
}
```

### Кастомный рендерер элементов

```javascript
// Расширение marked renderer
const renderer = new marked.Renderer();

renderer.image = (href, title, text) => {
    return `<figure>
        <img src="${href}" alt="${text}" />
        <figcaption>${text}</figcaption>
    </figure>`;
};

marked.use({ renderer });
```

## Производительность

### Оптимизация

- **Lazy loading** изображений (`loading="lazy"`)
- **Кэширование** markdown → HTML (можно добавить)
- **Асинхронная** загрузка MathJax (CDN)
- **Debouncing** для scroll tracking

### Рекомендации

- Ограничивать размер изображений (<500KB)
- Использовать WebP формат когда возможно
- Минимизировать количество формул на странице
- Оптимизировать размер markdown файлов (<100KB)

## Отладка

### Debug режим

```javascript
const parser = new ArticleParser({ debug: true });
// Выводит логи в консоль на каждом этапе
```

### Проверка парсинга

```javascript
console.log('Original:', markdownText);
console.log('Parsed:', html);
console.log('Headings:', toc.headings);
```

### Типичные проблемы

| Проблема | Причина | Решение |
|----------|---------|---------|
| Формулы не отображаются | MathJax не загружен | Проверить CDN, вызвать `ArticleParser.initMathJax()` |
| Изображения 404 | Неверный путь | Проверить структуру папок, basePath |
| Ссылки не работают | Нет `data-link` | Убедиться что ссылки на .md файлы |
| Содержание пустое | Мало заголовков | Снизить `minHeadings` |

## Тестирование

### Ручное тестирование

1. Создать тестовую статью в `articles/test-simple/`
2. Открыть `/article/test-simple`
3. Проверить все элементы:
   - Заголовки и содержание
   - Формулы (inline и block)
   - Специальные блоки
   - Изображения
   - Ссылки

### Автоматическое тестирование

```javascript
// Пример теста
async function testParser() {
    const parser = new ArticleParser();
    const md = '# Test\n\n$$E = mc^2$$';
    const html = await parser.parse(md);
    
    console.assert(html.includes('<h1'), 'H1 parsed');
    console.assert(html.includes('E = mc'), 'Formula parsed');
}
```

## Миграция существующих статей

См. отдельное руководство [ARTICLE_WRITING_GUIDE.md](ARTICLE_WRITING_GUIDE.md).

## Поддержка и вопросы

При возникновении вопросов или проблем:
1. Проверить [ARTICLE_PROTOCOL.md](ARTICLE_PROTOCOL.md)
2. Изучить примеры в `articles/`
3. Проверить консоль браузера на ошибки

## Заключение

Article Parser система предоставляет полноценное решение для публикации научных статей с поддержкой математики, специальных блоков и адаптивного дизайна. Система легко расширяется и интегрируется с существующим SPA роутером проекта.
