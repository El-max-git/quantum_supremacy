# 📚 Полезные команды Git

Справочник по основным командам Git для работы с проектом Quantum Supremacy.

---

## 📋 Содержание

- [Начальная настройка](#начальная-настройка)
- [Базовые команды](#базовые-команды)
- [Работа с ветками](#работа-с-ветками)
- [Работа с удаленным репозиторием](#работа-с-удаленным-репозиторием)
- [Отмена изменений](#отмена-изменений)
- [Просмотр истории](#просмотр-истории)
- [Работа с тегами](#работа-с-тегами)
- [Продвинутые команды](#продвинутые-команды)
- [Полезные алиасы](#полезные-алиасы)
- [Решение проблем](#решение-проблем)

---

## 🔧 Начальная настройка

### Настройка имени и email

```bash
# Установить имя пользователя
git config --global user.name "Ваше Имя"

# Установить email
git config --global user.email "your.email@example.com"

# Проверить настройки
git config --list
```

### Инициализация репозитория

```bash
# Создать новый репозиторий
git init

# Клонировать существующий репозиторий
git clone https://github.com/username/repo.git

# Клонировать в определенную папку
git clone https://github.com/username/repo.git my-folder
```

---

## 📝 Базовые команды

### Проверка статуса

```bash
# Полный статус
git status

# Краткий статус
git status -s

# Проверить состояние рабочей директории
git status --short
```

### Добавление файлов

```bash
# Добавить все изменения
git add .

# Добавить конкретный файл
git add index.html

# Добавить все файлы определенного типа
git add *.css

# Добавить папку
git add css/

# Добавить файлы в интерактивном режиме
git add -i

# Добавить изменения частично (patch mode)
git add -p
```

### Коммиты

```bash
# Создать коммит с сообщением
git commit -m "feat: добавлена новая функция"

# Добавить все изменения и сделать коммит
git commit -am "fix: исправлена ошибка"

# Изменить последний коммит (до push)
git commit --amend -m "feat: обновленное сообщение"

# Добавить файлы к последнему коммиту
git add forgotten-file.txt
git commit --amend --no-edit

# Создать пустой коммит (полезно для CI/CD)
git commit --allow-empty -m "chore: trigger CI"
```

### Удаление и перемещение файлов

```bash
# Удалить файл из Git и файловой системы
git rm file.txt

# Удалить файл только из Git (оставить в файловой системе)
git rm --cached file.txt

# Переименовать/переместить файл
git mv old-name.txt new-name.txt
```

---

## 🌿 Работа с ветками

### Создание и переключение

```bash
# Посмотреть все ветки
git branch

# Посмотреть все ветки (включая удаленные)
git branch -a

# Создать новую ветку
git branch feature/new-feature

# Переключиться на ветку
git checkout feature/new-feature

# Создать и сразу переключиться на новую ветку
git checkout -b feature/new-feature

# Новый способ (Git 2.23+)
git switch feature/new-feature

# Создать и переключиться (новый способ)
git switch -c feature/new-feature
```

### Слияние веток

```bash
# Слить ветку в текущую
git merge feature/new-feature

# Слияние без fast-forward (создает merge commit)
git merge --no-ff feature/new-feature

# Отменить слияние (если еще не закоммичено)
git merge --abort
```

### Удаление веток

```bash
# Удалить локальную ветку (только если уже слита)
git branch -d feature/old-feature

# Принудительно удалить ветку
git branch -D feature/old-feature

# Удалить удаленную ветку
git push origin --delete feature/old-feature
```

### Переименование ветки

```bash
# Переименовать текущую ветку
git branch -m new-name

# Переименовать другую ветку
git branch -m old-name new-name

# Переименовать master в main
git branch -M main
```

---

## 🌐 Работа с удаленным репозиторием

### Настройка удаленного репозитория

```bash
# Добавить удаленный репозиторий
git remote add origin https://github.com/username/repo.git

# Посмотреть удаленные репозитории
git remote -v

# Изменить URL удаленного репозитория
git remote set-url origin https://github.com/username/new-repo.git

# Удалить удаленный репозиторий
git remote remove origin
```

### Отправка изменений (Push)

```bash
# Отправить изменения в удаленный репозиторий
git push origin main

# Отправить и установить upstream (первый раз)
git push -u origin main

# Отправить все ветки
git push --all

# Отправить теги
git push --tags

# Принудительная отправка (осторожно!)
git push --force

# Более безопасная принудительная отправка
git push --force-with-lease
```

### Получение изменений (Pull/Fetch)

```bash
# Получить и слить изменения
git pull

# Получить изменения из конкретной ветки
git pull origin main

# Получить изменения с rebase
git pull --rebase

# Только получить изменения (без слияния)
git fetch

# Получить изменения из всех удаленных репозиториев
git fetch --all

# Получить и удалить удаленные ветки
git fetch --prune
```

---

## ↩️ Отмена изменений

### Отмена изменений в рабочей директории

```bash
# Отменить изменения в конкретном файле
git checkout -- file.txt

# Новый способ (Git 2.23+)
git restore file.txt

# Отменить все изменения
git checkout -- .
git restore .

# Удалить неотслеживаемые файлы (dry run)
git clean -n

# Удалить неотслеживаемые файлы
git clean -f

# Удалить неотслеживаемые файлы и папки
git clean -fd
```

### Отмена staging

```bash
# Убрать файл из staging (старый способ)
git reset HEAD file.txt

# Убрать файл из staging (новый способ)
git restore --staged file.txt

# Убрать все из staging
git reset HEAD
```

### Отмена коммитов

```bash
# Отменить последний коммит (сохранить изменения)
git reset --soft HEAD~1

# Отменить последний коммит (убрать из staging)
git reset HEAD~1

# Отменить последний коммит (удалить изменения)
git reset --hard HEAD~1

# Отменить 3 последних коммита
git reset --soft HEAD~3

# Создать новый коммит, отменяющий изменения
git revert HEAD

# Отменить конкретный коммит
git revert abc123
```

---

## 📖 Просмотр истории

### Просмотр логов

```bash
# Полная история
git log

# Краткая история (одна строка на коммит)
git log --oneline

# История с графом веток
git log --oneline --graph --all

# Последние N коммитов
git log -5

# История конкретного файла
git log -- file.txt

# История с изменениями
git log -p

# История с статистикой
git log --stat

# Красиво оформленный лог
git log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit
```

### Просмотр изменений

```bash
# Посмотреть незакоммиченные изменения
git diff

# Посмотреть изменения в staging
git diff --staged

# Сравнить две ветки
git diff main feature/new-feature

# Сравнить два коммита
git diff abc123 def456

# Посмотреть изменения в конкретном файле
git diff file.txt

# Статистика изменений
git diff --stat
```

### Поиск

```bash
# Найти коммит по сообщению
git log --grep="fix"

# Найти коммиты конкретного автора
git log --author="Имя"

# Найти по содержимому
git log -S "function name"

# Поиск в файлах
git grep "search term"
```

---

## 🏷️ Работа с тегами

```bash
# Посмотреть все теги
git tag

# Создать легковесный тег
git tag v1.0.0

# Создать аннотированный тег
git tag -a v1.0.0 -m "Версия 1.0.0"

# Создать тег для старого коммита
git tag -a v0.9.0 abc123 -m "Версия 0.9.0"

# Отправить тег на удаленный репозиторий
git push origin v1.0.0

# Отправить все теги
git push --tags

# Удалить локальный тег
git tag -d v1.0.0

# Удалить удаленный тег
git push origin --delete v1.0.0

# Переключиться на тег
git checkout v1.0.0
```

---

## 🚀 Продвинутые команды

### Rebase

```bash
# Rebase текущей ветки на main
git rebase main

# Интерактивный rebase (изменить последние 3 коммита)
git rebase -i HEAD~3

# Продолжить rebase после разрешения конфликтов
git rebase --continue

# Пропустить коммит при rebase
git rebase --skip

# Отменить rebase
git rebase --abort
```

### Stash (временное сохранение)

```bash
# Сохранить изменения
git stash

# Сохранить с описанием
git stash save "WIP: работа над функцией"

# Посмотреть список stash
git stash list

# Применить последний stash
git stash apply

# Применить и удалить последний stash
git stash pop

# Применить конкретный stash
git stash apply stash@{1}

# Удалить stash
git stash drop stash@{0}

# Удалить все stash
git stash clear

# Создать ветку из stash
git stash branch new-branch-name
```

### Cherry-pick

```bash
# Применить конкретный коммит к текущей ветке
git cherry-pick abc123

# Применить несколько коммитов
git cherry-pick abc123 def456

# Применить диапазон коммитов
git cherry-pick abc123..def456
```

### Bisect (поиск проблемного коммита)

```bash
# Начать bisect
git bisect start

# Отметить текущий коммит как плохой
git bisect bad

# Отметить коммит как хороший
git bisect good abc123

# После тестирования каждой версии
git bisect good  # или bad

# Завершить bisect
git bisect reset
```

### Submodules

```bash
# Добавить submodule
git submodule add https://github.com/user/repo.git path/to/submodule

# Инициализировать submodules
git submodule init

# Обновить submodules
git submodule update

# Клонировать репозиторий с submodules
git clone --recursive https://github.com/user/repo.git

# Обновить все submodules
git submodule update --remote
```

---

## 🎯 Полезные алиасы

Добавьте в `.gitconfig`:

```bash
# Настроить алиасы
git config --global alias.st status
git config --global alias.co checkout
git config --global alias.br branch
git config --global alias.ci commit
git config --global alias.unstage 'reset HEAD --'
git config --global alias.last 'log -1 HEAD'
git config --global alias.visual 'log --oneline --graph --all'
git config --global alias.lg "log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit"
```

Теперь можно использовать:

```bash
git st        # вместо git status
git co main   # вместо git checkout main
git br        # вместо git branch
git ci -m "message"  # вместо git commit -m "message"
git unstage file.txt  # убрать из staging
git last      # последний коммит
git visual    # красивый граф
git lg        # подробный лог с графом
```

---

## 🔧 Решение проблем

### Конфликты при слиянии

```bash
# Посмотреть конфликтующие файлы
git status

# После разрешения конфликтов
git add resolved-file.txt
git commit

# Отменить слияние
git merge --abort
```

### Случайный коммит в main

```bash
# Вернуть коммит на новую ветку
git branch feature/new-feature
git reset --hard HEAD~1
git checkout feature/new-feature
```

### Забыли переключиться на новую ветку

```bash
# Сохранить изменения
git stash

# Создать и переключиться на новую ветку
git checkout -b feature/new-feature

# Вернуть изменения
git stash pop
```

### Случайно удалили коммит

```bash
# Найти потерянный коммит
git reflog

# Восстановить коммит
git checkout abc123
# или
git cherry-pick abc123
```

### Очистка репозитория

```bash
# Удалить старые ветки, которых больше нет на удаленном
git remote prune origin

# Оптимизировать репозиторий
git gc

# Проверить целостность репозитория
git fsck
```

### Изменить автора коммита

```bash
# Последнего коммита
git commit --amend --author="Имя Фамилия <email@example.com>"

# Изменить несколько коммитов
git rebase -i HEAD~3
# В редакторе заменить pick на edit для нужных коммитов
# Затем для каждого:
git commit --amend --author="Имя Фамилия <email@example.com>"
git rebase --continue
```

---

## 📚 Дополнительные ресурсы

### Документация

- [Официальная документация Git](https://git-scm.com/doc)
- [Git Book на русском](https://git-scm.com/book/ru/v2)
- [GitHub Docs](https://docs.github.com/)

### Интерактивное обучение

- [Learn Git Branching](https://learngitbranching.js.org/?locale=ru_RU)
- [Git How To](https://githowto.com/ru)

### Шпаргалки

- [Git Cheat Sheet (GitHub)](https://education.github.com/git-cheat-sheet-education.pdf)
- [Visual Git Cheat Sheet](https://ndpsoftware.com/git-cheatsheet.html)

---

## 💡 Лучшие практики

### Сообщения коммитов

Используйте префиксы (Conventional Commits):

- `feat:` - новая функция
- `fix:` - исправление ошибки
- `docs:` - изменения в документации
- `style:` - форматирование кода
- `refactor:` - рефакторинг
- `test:` - добавление тестов
- `chore:` - обновление зависимостей, конфигурации

**Примеры:**

```bash
git commit -m "feat: добавлена форма регистрации"
git commit -m "fix: исправлена ошибка в навигации"
git commit -m "docs: обновлено README"
```

### Частота коммитов

✅ **Делайте:**
- Частые небольшие коммиты
- Коммиты логически связанных изменений
- Коммиты с понятными сообщениями

❌ **Избегайте:**
- Гигантских коммитов с множеством изменений
- Коммитов незавершенного кода в main
- Бесполезных сообщений типа "fix", "update"

### Работа с ветками

✅ **Правильно:**
```bash
main                    # стабильная версия
develop                 # разработка
feature/user-auth       # новая функция
fix/login-bug           # исправление бага
hotfix/critical-issue   # срочное исправление
```

❌ **Избегайте:**
```bash
test
new
my-changes
123
```

---

**Удачи в работе с Git! 🚀**

Если возникли вопросы, создайте [Issue](https://github.com/el-max-git/quantum_supremacy/issues).

