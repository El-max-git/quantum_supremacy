# 🚀 Руководство по настройке проекта

Это руководство поможет вам настроить проект Quantum Supremacy на вашем локальном компьютере.

## 📋 Предварительные требования

Для работы с проектом вам понадобится:
- Современный веб-браузер (Chrome, Firefox, Safari, Edge)
- Текстовый редактор (рекомендуется VS Code или Cursor)
- Git для контроля версий
- (Опционально) Node.js для локального сервера

## 🛠️ Установка

### 1. Клонирование репозитория

```bash
# Клонируйте репозиторий
git clone https://github.com/el-max-git/quantum_supremacy.git

# Перейдите в директорию проекта
cd quantum_supremacy
```

### 2. Структура проекта

После клонирования вы увидите следующую структуру:

```
quantum_supremacy/
├── .github/              # GitHub конфигурация
│   ├── workflows/        # GitHub Actions
│   └── ISSUE_TEMPLATE/   # Шаблоны для Issues
├── assets/               # Медиа файлы
│   ├── images/          # Изображения
│   └── icons/           # Иконки
├── css/                 # Стили
│   └── style.css
├── js/                  # JavaScript
│   └── main.js
├── docs/                # Документация
├── index.html           # Главная страница
├── .cursorrules         # Правила для Cursor AI
├── .gitignore          # Git игнорируемые файлы
├── README.md           # Основное README
├── LICENSE             # Лицензия MIT
└── CONTRIBUTING.md     # Руководство по вкладу
```

## 🚀 Запуск проекта

### Вариант 1: Открытие напрямую

Самый простой способ - просто открыть файл `index.html` в браузере:

1. Найдите файл `index.html` в проводнике
2. Щелкните правой кнопкой мыши
3. Выберите "Открыть с помощью" → Ваш браузер

### Вариант 2: Live Server (рекомендуется)

Если вы используете VS Code или Cursor:

1. Установите расширение "Live Server"
2. Откройте проект в редакторе
3. Щелкните правой кнопкой на `index.html`
4. Выберите "Open with Live Server"

Сайт откроется по адресу `http://localhost:5500` с автоматической перезагрузкой при изменениях.

### Вариант 3: Python HTTP Server

Если у вас установлен Python:

```bash
# Python 3
python -m http.server 8000

# Или Python 2
python -m SimpleHTTPServer 8000
```

Откройте браузер и перейдите по адресу `http://localhost:8000`

### Вариант 4: Node.js HTTP Server

Если у вас установлен Node.js:

```bash
# Установите http-server глобально (один раз)
npm install -g http-server

# Запустите сервер
http-server -p 8000
```

## 🛠️ Разработка

### Редактирование файлов

1. **HTML** (`index.html`) - структура страницы
2. **CSS** (`css/style.css`) - стили и дизайн
3. **JavaScript** (`js/main.js`) - интерактивность

### Добавление изображений

1. Поместите изображения в `assets/images/`
2. Оптимизируйте их перед добавлением (используйте TinyPNG или аналоги)
3. Используйте в HTML:

```html
<img src="assets/images/your-image.jpg" alt="Описание">
```

### Работа с Git

```bash
# Проверка статуса
git status

# Добавление изменений
git add .

# Коммит
git commit -m "feat: описание изменений"

# Отправка на GitHub
git push origin main
```

## 🌐 Развертывание на GitHub Pages

1. Перейдите в Settings вашего репозитория на GitHub
2. Выберите "Pages" в левом меню
3. В разделе "Source" выберите "gh-pages" branch (будет создана автоматически через GitHub Actions)
4. Сохраните изменения
5. Ваш сайт будет доступен по адресу: `https://el-max-git.github.io/quantum_supremacy/`

## 🔧 Отладка

### Проблемы с отображением

1. Откройте консоль браузера (F12)
2. Проверьте вкладку "Console" на наличие ошибок
3. Проверьте вкладку "Network" для проблем с загрузкой ресурсов

### Проблемы с Git

```bash
# Если изменения не отправляются
git pull origin main --rebase
git push origin main
```

## 📚 Дополнительные ресурсы

- [HTML документация (MDN)](https://developer.mozilla.org/ru/docs/Web/HTML)
- [CSS документация (MDN)](https://developer.mozilla.org/ru/docs/Web/CSS)
- [JavaScript документация (MDN)](https://developer.mozilla.org/ru/docs/Web/JavaScript)
- [Git документация](https://git-scm.com/book/ru/v2)

## ❓ Нужна помощь?

Если у вас возникли проблемы:

1. Проверьте [Issues](https://github.com/el-max-git/quantum_supremacy/issues) на наличие похожих проблем
2. Создайте новый Issue с подробным описанием проблемы
3. Прочитайте [CONTRIBUTING.md](../CONTRIBUTING.md) для получения дополнительной информации

Удачи в разработке! 🎉

