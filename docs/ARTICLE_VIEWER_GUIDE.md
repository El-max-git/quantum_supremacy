# Article Viewer Module - Руководство

## Описание

`ArticleViewer` - универсальный JavaScript модуль для отображения списка научных статей и их просмотра. Работает с системой парсера статей и автоматически подгружает контент из папки `articles/`.

## Возможности

✅ **Автоматический список** - загружает статьи из `config.json`  
✅ **Группировка по категориям** - визуальная организация  
✅ **Карточки статей** - с превью, тегами, метаданными  
✅ **Клик для просмотра** - открывает статью в том же модуле  
✅ **Полный функционал** - TOC, формулы, специальные блоки  
✅ **История браузера** - кнопка "Назад" работает корректно  
✅ **Прямые ссылки** - `/articles?id=article-id`  
✅ **Адаптивный дизайн** - mobile + desktop  
✅ **Темная/светлая тема** - автоматическая поддержка  

## Архитектура

```
┌─────────────────────────────────┐
│     pages/articles.html         │  ← Страница со списком
│  ┌───────────┐  ┌─────────────┐ │
│  │ List View │  │ Article View│ │  ← Два режима
│  └───────────┘  └─────────────┘ │
└─────────────────────────────────┘
           ↓
┌─────────────────────────────────┐
│   js/article-viewer.js          │  ← Универсальный модуль
│  • loadArticles()               │
│  • renderArticlesList()         │
│  • viewArticle(id)              │
│  • backToList()                 │
└─────────────────────────────────┘
           ↓
┌─────────────────────────────────┐
│   js/article-parser.js          │  ← Парсер
│  • parse(markdown)              │
│  • extractFrontmatter()         │
└─────────────────────────────────┘
           ↓
┌─────────────────────────────────┐
│   articles/*/article.md         │  ← Контент
│  (YAML frontmatter + Markdown)  │
└─────────────────────────────────┘
```

## Использование

### Базовая инициализация

```javascript
const viewer = new ArticleViewer({
    basePath: '/quantum_supremacy',
    listContainerId: 'articles-list',
    viewerContainerId: 'article-viewer',
    configPath: '/data/config.json'
});

await viewer.init();
```

### Параметры конфигурации

| Параметр | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| `basePath` | string | `''` | Базовый путь для GitHub Pages |
| `listContainerId` | string | `'articles-list'` | ID контейнера списка |
| `viewerContainerId` | string | `'article-viewer'` | ID контейнера просмотра |
| `configPath` | string | `'/data/config.json'` | Путь к конфигурации |

### HTML структура

```html
<div id="articles-list">
    <!-- Список статей будет здесь -->
</div>

<div id="article-viewer" style="display: none;">
    <!-- Просмотр статьи будет здесь -->
</div>
```

## API модуля

### Методы

#### `init()`
Инициализация модуля - загружает конфигурацию и отображает список.

```javascript
await viewer.init();
```

#### `loadArticles()`
Загружает список статей из `config.json`.

```javascript
const articles = await viewer.loadArticles();
```

#### `renderArticlesList()`
Рендерит список статей с карточками.

```javascript
viewer.renderArticlesList();
```

#### `viewArticle(articleId)`
Открывает статью для просмотра.

```javascript
await viewer.viewArticle('test-simple');
```

#### `backToList()`
Возвращается к списку статей.

```javascript
viewer.backToList();
```

#### `showError(message)`
Отображает сообщение об ошибке.

```javascript
viewer.showError('Статья не найдена');
```

## Работа с URL

### Прямые ссылки

Модуль поддерживает прямые ссылки на статьи:

```
/articles                    → список статей
/articles?id=test-simple     → конкретная статья
```

### История браузера

- ✅ Кнопка "Назад" работает корректно
- ✅ URL обновляется при просмотре статьи
- ✅ `window.history.pushState()` для SPA навигации

## Стилизация

### CSS классы

| Класс | Описание |
|-------|----------|
| `.articles-grid` | Сетка статей |
| `.category-section` | Секция категории |
| `.category-title` | Заголовок категории |
| `.articles-cards` | Grid контейнер карточек |
| `.article-card` | Карточка статьи |
| `.article-card-header` | Заголовок карточки |
| `.article-card-title` | Название статьи |
| `.article-card-tags` | Теги статьи |
| `.article-card-body` | Описание статьи |
| `.article-card-footer` | Метаданные (автор, дата) |
| `.back-to-list-btn` | Кнопка "Назад к списку" |
| `.loading-indicator` | Индикатор загрузки |
| `.article-error` | Сообщение об ошибке |

### Кастомизация темы

Модуль использует CSS переменные:

```css
:root {
    --bg-primary: #ffffff;
    --bg-secondary: #f8fafc;
    --text-primary: #1e293b;
    --text-secondary: #64748b;
    --border-color: #e2e8f0;
    --primary-color: #3b82f6;
}

@media (prefers-color-scheme: dark) {
    :root {
        --bg-primary: #0f172a;
        --bg-secondary: #1e293b;
        --text-primary: #f8fafc;
        --text-secondary: #94a3b8;
        --border-color: #334155;
    }
}
```

## Интеграция с роутером

### Добавление маршрута

В `data/config.json`:

```json
{
  "routes": [
    {
      "path": "/articles",
      "page": "articles.html",
      "title": "Статьи - Quantum Supremacy",
      "inMenu": true,
      "order": 4
    }
  ]
}
```

### В `index.html`

```html
<script src="js/article-viewer.js"></script>
```

## Пример полной настройки

### 1. Создать страницу `pages/articles.html`

```html
<div class="container">
    <h1>📚 Научные статьи</h1>
    
    <div id="articles-list"></div>
    <div id="article-viewer" style="display: none;"></div>
</div>

<script>
(async function() {
    const viewer = new ArticleViewer({
        basePath: window.router?.basePath || '',
        listContainerId: 'articles-list',
        viewerContainerId: 'article-viewer'
    });
    
    window.articleViewer = viewer;
    await viewer.init();
    
    // Обработка истории браузера
    window.addEventListener('popstate', () => {
        const urlParams = new URLSearchParams(window.location.search);
        const articleId = urlParams.get('id');
        
        if (articleId) {
            viewer.viewArticle(articleId);
        } else {
            viewer.backToList();
        }
    });
})();
</script>
```

### 2. Добавить статьи в `config.json`

```json
{
  "articles": [
    {
      "id": "test-simple",
      "title": "Простая тестовая статья",
      "path": "/article/test-simple",
      "mdFile": "articles/test-simple/article.md",
      "category": "Тестирование",
      "tags": ["тест", "markdown"],
      "date": "2025-12-31",
      "author": "AI Assistant"
    }
  ]
}
```

### 3. Готово! ✅

Откройте `/articles` и увидите список статей.

## Решение проблем

| Проблема | Решение |
|----------|---------|
| Список не загружается | Проверьте `config.json` и наличие секции `articles` |
| Статья не открывается | Проверьте путь `mdFile` в конфиге |
| Формулы не рендерятся | Убедитесь что MathJax загружен в `index.html` |
| Стили не применяются | Проверьте CSS переменные и `style.css` |
| История не работает | Проверьте обработчик `popstate` |

## Преимущества

✅ **Универсальность** - один модуль для всех статей  
✅ **Простота** - минимум кода для интеграции  
✅ **Автоматизация** - список генерируется автоматически  
✅ **SPA опыт** - без перезагрузки страницы  
✅ **SEO friendly** - прямые ссылки на статьи  
✅ **Расширяемость** - легко добавлять новые возможности  

## Дальнейшее развитие

- [ ] Фильтрация по тегам
- [ ] Поиск по статьям
- [ ] Сортировка (по дате, названию, популярности)
- [ ] Пагинация для большого количества статей
- [ ] Режим чтения (без отвлекающих элементов)
- [ ] Сохранение прогресса чтения
- [ ] Экспорт статьи в PDF

---

**Автор модуля:** AI Assistant  
**Дата создания:** 31.12.2025  
**Версия:** 1.0.0
