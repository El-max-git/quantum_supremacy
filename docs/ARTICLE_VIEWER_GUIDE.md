# Article Viewer Module - Руководство

## Описание

Система просмотра статей состоит из двух компонентов:
- **ArticlesCatalog** (`js/articles-catalog.js`) - модуль для отображения каталога статей с навигацией по директориям
- **Article Viewer Script** (встроен в `pages/article-view.html`) - модуль для загрузки, парсинга и отображения отдельных статей

Система автоматически подгружает контент из папки `articles/` на основе структуры `articles/articles-list.json`.

## Возможности

✅ **Автоматический список** - загружает статьи из `config.json`  
✅ **Группировка по категориям** - визуальная организация  
✅ **Карточки статей** - с превью, тегами, метаданными  
✅ **Клик для просмотра** - открывает статью в том же модуле  
✅ **Полный функционал** - TOC, формулы, специальные блоки  
✅ **История браузера** - кнопка "Назад" работает корректно  
✅ **Прямые ссылки** - `/articles?id=article-id`  
✅ **Адаптивный дизайн** - mobile + desktop  
✅ **Темная/светлая тема** - автоматическая поддержка  

## Архитектура

```
┌─────────────────────────────────┐
│     pages/articles.html         │  ← Страница каталога
│  ┌─────────────────────────────┐ │
│  │ <div id="articles-list">     │ │  ← Каталог статей
│  └─────────────────────────────┘ │
└─────────────────────────────────┘
           ↓
┌─────────────────────────────────┐
│   js/articles-catalog.js        │  ← Модуль каталога
│   (ArticlesCatalog)              │
│  • init()                        │
│  • renderCatalog()               │
│  • navigateToDirectory()         │
│  • openArticle()                 │
└─────────────────────────────────┘
           ↓
┌─────────────────────────────────┐
│   pages/article-view.html        │  ← Страница просмотра
│   (встроенный скрипт)           │
│  • loadArticle()                 │
│  • simpleMarkdownToHtml()        │
│  • paginateArticle()             │
│  • generateTOC()                 │
└─────────────────────────────────┘
           ↓
┌─────────────────────────────────┐
│   articles/articles-list.json    │  ← Каталог статей
│   articles/{id}/article.md       │  ← Контент
│  (YAML frontmatter + Markdown)  │
└─────────────────────────────────┘
```

## Использование

### Базовая инициализация каталога

```javascript
const catalog = new ArticlesCatalog({
    basePath: '/quantum_supremacy',
    listContainerId: 'articles-list',
    configPath: '/articles/articles-list.json'
});

await catalog.init();
```

### Параметры конфигурации ArticlesCatalog

| Параметр | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| `basePath` | string | `''` | Базовый путь для GitHub Pages |
| `listContainerId` | string | `'articles-list'` | ID контейнера каталога |
| `configPath` | string | `'/articles/articles-list.json'` | Путь к каталогу статей |

### HTML структура для каталога

```html
<div id="articles-list">
    <!-- Каталог статей будет здесь -->
</div>
```

### HTML структура для просмотра статьи

Статья отображается на отдельной странице `pages/article-view.html`:

```html
<div id="article-content">
    <header class="article-header">
        <!-- Метаданные статьи -->
    </header>
    <div id="article-body">
        <!-- Контент статьи -->
    </div>
</div>
```

## API модуля ArticlesCatalog

### Методы

#### `init()`
Инициализация модуля - загружает каталог из JSON и отображает список.

```javascript
await catalog.init();
```

#### `renderCatalog()`
Рендерит каталог статей (директории и статьи текущего уровня).

```javascript
catalog.renderCatalog();
```

#### `navigateToDirectory(dirId)`
Переходит в указанную директорию.

```javascript
catalog.navigateToDirectory('cosmology');
```

#### `openArticle(articleId)`
Открывает статью для просмотра через роутер.

```javascript
catalog.openArticle('test-simple');
```

#### `showError(message)`
Отображает сообщение об ошибке.

```javascript
catalog.showError('Статья не найдена');
```

## Работа с URL

### Прямые ссылки

Система поддерживает прямые ссылки на статьи:

```
/articles                    → каталог статей
/article?id=test-simple      → конкретная статья
/article?id=test-simple&page=2 → статья, страница 2
```

### История браузера

- ✅ Кнопка "Назад" работает корректно
- ✅ URL обновляется при просмотре статьи
- ✅ `window.history.pushState()` для SPA навигации

## Стилизация

### CSS классы

| Класс | Описание |
|-------|----------|
| `.articles-grid` | Сетка статей |
| `.category-section` | Секция категории |
| `.category-title` | Заголовок категории |
| `.articles-cards` | Grid контейнер карточек |
| `.article-card` | Карточка статьи |
| `.article-card-header` | Заголовок карточки |
| `.article-card-title` | Название статьи |
| `.article-card-tags` | Теги статьи |
| `.article-card-body` | Описание статьи |
| `.article-card-footer` | Метаданные (автор, дата) |
| `.back-to-list-btn` | Кнопка "Назад к списку" |
| `.loading-indicator` | Индикатор загрузки |
| `.article-error` | Сообщение об ошибке |

### Кастомизация темы

Модуль использует CSS переменные:

```css
:root {
    --bg-primary: #ffffff;
    --bg-secondary: #f8fafc;
    --text-primary: #1e293b;
    --text-secondary: #64748b;
    --border-color: #e2e8f0;
    --primary-color: #3b82f6;
}

@media (prefers-color-scheme: dark) {
    :root {
        --bg-primary: #0f172a;
        --bg-secondary: #1e293b;
        --text-primary: #f8fafc;
        --text-secondary: #94a3b8;
        --border-color: #334155;
    }
}
```

## Интеграция с роутером

### Добавление маршрута

В `data/config.json`:

```json
{
  "routes": [
    {
      "path": "/articles",
      "page": "articles.html",
      "title": "Статьи - Quantum Supremacy",
      "inMenu": true,
      "order": 4
    },
    {
      "path": "/article",
      "page": "article-view.html",
      "title": "Просмотр статьи - Quantum Supremacy",
      "inMenu": false
    }
  ]
}
```

### В `index.html`

```html
<script src="js/articles-catalog.js"></script>
<script src="js/table-of-contents.js"></script>
```

## Пример полной настройки

### 1. Структура каталога статей

Создайте файл `articles/articles-list.json`:

```json
{
  "categories": [
    {
      "id": "test",
      "title": "Тестовые статьи",
      "description": "Тестовые и примеры статей",
      "icon": "🧪",
      "order": 1,
      "items": [
        {
          "id": "test-simple",
          "mdFile": "articles/test-simple/article.md"
        }
      ]
    }
  ]
}
```

### 2. Создать страницу `pages/articles.html`

```html
<div class="container">
    <h1>📚 Научные статьи</h1>
    <div id="articles-list"></div>
</div>

<script>
(async function() {
    const catalog = new ArticlesCatalog({
        basePath: window.router?.basePath || '',
        listContainerId: 'articles-list',
        configPath: '/articles/articles-list.json'
    });
    
    window.articlesCatalog = catalog;
    await catalog.init();
})();
</script>
```

### 3. Создать статью

Создайте файл `articles/test-simple/article.md`:

```markdown
---
id: test-simple
title: Простая тестовая статья
author: AI Assistant
date: 2025-01-01
category: Тестирование
tags: [тест, markdown]
description: Тестовая статья
readingTime: 5
---

# Простая тестовая статья

Текст статьи...
```

### 4. Готово! ✅

Откройте `/articles` и увидите каталог статей. Клик по статье откроет её на странице `/article?id=test-simple`.

## Решение проблем

| Проблема | Решение |
|----------|---------|
| Каталог не загружается | Проверьте `articles/articles-list.json` и наличие секции `categories` |
| Статья не открывается | Проверьте путь `mdFile` в каталоге и существование файла |
| Формулы не рендерятся | Убедитесь что MathJax загружен в `index.html` |
| Стили не применяются | Проверьте CSS переменные и `style.css` |
| Навигация не работает | Проверьте что роутер инициализирован и маршруты добавлены в `config.json` |

## Преимущества

✅ **Универсальность** - один модуль для всех статей  
✅ **Простота** - минимум кода для интеграции  
✅ **Автоматизация** - каталог генерируется из JSON  
✅ **SPA опыт** - без перезагрузки страницы  
✅ **SEO friendly** - прямые ссылки на статьи  
✅ **Пагинация** - автоматическая разбивка длинных статей на страницы  
✅ **Интерактивное содержание** - автоматическая генерация TOC  
✅ **Расширяемость** - легко добавлять новые возможности  

## Дальнейшее развитие

- [ ] Фильтрация по тегам
- [ ] Поиск по статьям
- [ ] Сортировка (по дате, названию, популярности)
- [ ] Режим чтения (без отвлекающих элементов)
- [ ] Сохранение прогресса чтения
- [ ] Экспорт статьи в PDF

---

**Автор модуля:** AI Assistant  
**Дата обновления:** 2025-01-01  
**Версия:** 2.0.0
