# Древовидная структура статей

## Обзор

Система поддерживает древовидную организацию статей по категориям и подкатегориям. Это позволяет группировать статьи по темам, создавать иерархию и упрощать навигацию.

## Структура JSON

### Формат файла `articles/articles-list.json`

```json
{
  "structure": "tree",
  "version": "2.0",
  "lastUpdated": "2025-01-01T00:00:00.000Z",
  "categories": [
    {
      "id": "cosmology",
      "title": "Космология",
      "description": "Статьи о космологии, Вселенной и космических явлениях",
      "icon": "🌌",
      "order": 1,
      "items": [
        {
          "type": "article",
          "id": "expanding-universe-hypersphere",
          "mdFile": "articles/EXPANDING_UNIVERSE_HYPERSPHERE_/EXPANDING_UNIVERSE_HYPERSPHERE_.md"
        },
        {
          "type": "category",
          "id": "big-bang",
          "title": "Теория Большого Взрыва",
          "description": "Статьи о происхождении Вселенной",
          "icon": "💥",
          "order": 1,
          "items": [
            {
              "type": "article",
              "id": "big-bang-theory",
              "mdFile": "articles/cosmology/big-bang/article.md"
            }
          ]
        }
      ]
    }
  ],
  "articles": [...],
  "count": 2
}
```

### Поля категории

| Поле | Тип | Обязательное | Описание |
|------|-----|--------------|----------|
| `id` | string | Да | Уникальный идентификатор категории |
| `title` | string | Да | Название категории |
| `description` | string | Нет | Описание категории |
| `icon` | string | Нет | Эмодзи-иконка категории |
| `order` | number | Нет | Порядок сортировки (меньше = выше) |
| `items` | array | Нет | Массив статей и подкатегорий |

### Поля элемента (статья или подкатегория)

| Поле | Тип | Обязательное | Описание |
|------|-----|--------------|----------|
| `type` | string | Да | `"article"` или `"category"` |
| `id` | string | Да | ID статьи или категории |
| `mdFile` | string | Только для статей | Путь к Markdown файлу |
| `title` | string | Только для категорий | Название подкатегории |
| `description` | string | Нет | Описание подкатегории |
| `icon` | string | Нет | Эмодзи-иконка подкатегории |
| `order` | number | Нет | Порядок сортировки |
| `items` | array | Только для категорий | Вложенные элементы |

## Автоматическая генерация

Скрипт `scripts/scan-articles.js` автоматически создает древовидную структуру на основе:

1. **Категории из frontmatter** - поле `category` в YAML frontmatter статьи
2. **Структуры папок** - если категория не указана, используется имя папки

### Пример структуры папок

```
articles/
├── cosmology/
│   ├── big-bang/
│   │   └── article.md
│   └── expanding-universe/
│       └── article.md
├── quantum-physics/
│   └── article.md
└── test/
    └── article.md
```

Будет автоматически преобразовано в:

```json
{
  "categories": [
    {
      "id": "cosmology",
      "title": "Cosmology",
      "items": [
        {
          "type": "category",
          "id": "big-bang",
          "title": "Big Bang",
          "items": [...]
        }
      ]
    }
  ]
}
```

## Ручное редактирование

Вы можете вручную редактировать `articles/articles-list.json` для:

- Добавления описаний категорий
- Изменения иконок
- Изменения порядка категорий
- Создания подкатегорий
- Перемещения статей между категориями

### Пример добавления подкатегории

```json
{
  "id": "cosmology",
  "title": "Космология",
  "items": [
    {
      "type": "category",
      "id": "dark-matter",
      "title": "Темная материя",
      "description": "Исследования темной материи",
      "icon": "🌑",
      "order": 1,
      "items": [
        {
          "type": "article",
          "id": "dark-matter-detection",
          "mdFile": "articles/cosmology/dark-matter/article.md"
        }
      ]
    }
  ]
}
```

## Обратная совместимость

Система поддерживает оба формата:

1. **Древовидная структура** (`structure: "tree"`) - новый формат
2. **Плоская структура** (без `structure` или `structure: "flat"`) - старый формат

При использовании плоской структуры статьи группируются по полю `category` из метаданных.

## Иконки категорий

По умолчанию используются следующие иконки:

| Категория | Иконка |
|-----------|--------|
| cosmology | 🌌 |
| quantum-physics, quantum | ⚛️ |
| vacuum-energy, vacuum | ⚡ |
| physics | 🔬 |
| mathematics | 📐 |
| test | 🧪 |
| uncategorized | 📁 |

Вы можете изменить иконку, указав поле `icon` в JSON.

## Порядок сортировки

Категории и статьи сортируются по полю `order`:

- Меньшее значение = выше в списке
- Если `order` не указан, используется значение по умолчанию (999)
- Внутри одной категории сначала идут подкатегории, потом статьи

## Визуальное отображение

### Сворачивание/разворачивание

- Категории с элементами имеют кнопку ▼/▲ для сворачивания
- Клик по заголовку категории также сворачивает/разворачивает её
- Вложенные категории отображаются с отступом и левой границей

### Стилизация

- Категории верхнего уровня: крупный шрифт (1.8rem)
- Вложенные категории: уменьшенный шрифт (1.5rem, 1.25rem)
- Иконки категорий отображаются перед названием
- Описания категорий отображаются курсивом под заголовком

## Примеры использования

### Простая категория с статьями

```json
{
  "id": "quantum-physics",
  "title": "Квантовая физика",
  "description": "Статьи о квантовой механике",
  "icon": "⚛️",
  "order": 2,
  "items": [
    {
      "type": "article",
      "id": "quantum-entanglement",
      "mdFile": "articles/quantum-physics/entanglement/article.md"
    },
    {
      "type": "article",
      "id": "quantum-superposition",
      "mdFile": "articles/quantum-physics/superposition/article.md"
    }
  ]
}
```

### Категория с подкатегориями

```json
{
  "id": "cosmology",
  "title": "Космология",
  "items": [
    {
      "type": "category",
      "id": "big-bang",
      "title": "Теория Большого Взрыва",
      "items": [
        {
          "type": "article",
          "id": "big-bang-theory",
          "mdFile": "articles/cosmology/big-bang/theory/article.md"
        }
      ]
    },
    {
      "type": "category",
      "id": "dark-energy",
      "title": "Темная энергия",
      "items": [
        {
          "type": "article",
          "id": "dark-energy-expansion",
          "mdFile": "articles/cosmology/dark-energy/expansion/article.md"
        }
      ]
    }
  ]
}
```

## Обновление структуры

После изменения структуры папок или добавления новых статей:

```bash
node scripts/scan-articles.js
```

Скрипт автоматически:
1. Сканирует папку `articles/`
2. Извлекает категории из frontmatter или структуры папок
3. Обновляет `articles/articles-list.json`
4. Сохраняет существующие настройки (иконки, описания, порядок) если они есть

## Миграция со старого формата

Если у вас уже есть `articles-list.json` в старом формате:

1. Запустите `node scripts/scan-articles.js`
2. Скрипт автоматически создаст древовидную структуру
3. При необходимости отредактируйте JSON вручную для добавления описаний и иконок
