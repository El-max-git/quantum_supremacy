# 🔄 ДИНАМИЧЕСКИЕ МАРШРУТЫ

> **Система динамической загрузки карты сайта из JSON конфигурации**

---

## 📋 Конфигурация

### 1. Маршруты в JSON (`data/config.json`)

```json
{
  "routes": [
    {
      "path": "/",
      "page": "home.html",
      "title": "Главная",
      "inMenu": true
    },
    {
      "path": "/about",
      "page": "about.html",
      "title": "О проекте",
      "inMenu": true
    },
    {
      "path": "/technology",
      "page": "technology.html",
      "title": "Технологии",
      "inMenu": true
    },
    {
      "path": "/contact",
      "page": "contact.html",
      "title": "Контакты",
      "inMenu": true
    },
    {
      "path": "/404",
      "page": "404.html",
      "title": "Страница не найдена",
      "inMenu": false
    }
  ]
}
```

### 2. Автоматическая загрузка (`index.html`)

```javascript
async function initializeRoutes() {
    try {
        // Загружаем конфигурацию
        const response = await fetch(`${basePath}/data/config.json`);
        const config = await response.json();

        // Регистрируем маршруты автоматически
        config.routes.forEach(route => {
            router.addRoute(route.path, () => 
                loadPage(`${basePath}/pages/${route.page}`)
            );
        });

        router.start();
    } catch (error) {
        // Fallback на дефолтные маршруты
        registerDefaultRoutes();
        router.start();
    }
}
```

---

## 🎯 Преимущества

### 1. Единый источник правды

```
data/config.json
    ↓
┌───────────────┬───────────────┬───────────────┐
│   Router      │  Navigation   │  Sitemap.xml  │
│  Маршруты     │  Меню сайта   │  SEO карта    │
└───────────────┴───────────────┴───────────────┘
```

Все читают из одного файла конфигурации!

### 2. Простое добавление страниц

**Шаг 1:** Создать страницу

```bash
# Создаем новую страницу
echo "<h1>Новая страница</h1>" > pages/new-page.html
```

**Шаг 2:** Добавить в config.json

```json
{
  "path": "/new-page",
  "page": "new-page.html",
  "title": "Новая страница",
  "inMenu": true
}
```

**Готово!** Роутер автоматически подхватит новый маршрут.

### 3. Централизованное управление

```json
{
  "path": "/blog",
  "page": "blog.html",
  "title": "Блог",
  "inMenu": true,
  "meta": {
    "description": "Наш блог о квантовых технологиях",
    "keywords": ["quantum", "blog", "science"]
  },
  "permissions": ["public"],
  "analytics": true
}
```

Можно добавить любые дополнительные параметры!

### 4. Fallback механизм

Если `config.json` недоступен (сеть, ошибка), используются жестко закодированные дефолтные маршруты:

```javascript
function registerDefaultRoutes() {
    router.addRoute('/', () => loadPage('/pages/home.html'));
    router.addRoute('/about', () => loadPage('/pages/about.html'));
    // ... минимальный набор маршрутов
}
```

**Сайт всегда работает!** ✅

---

## 📊 Поток работы

```
┌─────────────────────────────────────────────────┐
│  1. Загрузка index.html                         │
└─────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────┐
│  2. Создание экземпляра Router                  │
│     const router = new Router();                │
└─────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────┐
│  3. Вызов initializeRoutes() [async]            │
└─────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────┐
│  4. Загрузка config.json                        │
│     fetch('/data/config.json')                  │
└─────────────────────────────────────────────────┘
         ↓ success                    ↓ error
┌──────────────────────┐    ┌──────────────────────┐
│ 5a. Парсинг JSON     │    │ 5b. Fallback         │
│     config.routes    │    │     registerDefault  │
└──────────────────────┘    └──────────────────────┘
         ↓                           ↓
┌─────────────────────────────────────────────────┐
│  6. Регистрация маршрутов                       │
│     config.routes.forEach(route => {            │
│       router.addRoute(route.path, handler)      │
│     })                                          │
└─────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────┐
│  7. Запуск роутера                              │
│     router.start()                              │
└─────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────┐
│  8. Загрузка текущей страницы                   │
│     loadRoute(currentPath)                      │
└─────────────────────────────────────────────────┘
                    ↓
              ✅ Готово!
```

---

## 🚀 Дополнительные возможности

### Автогенерация навигации

**Файл:** `js/navigation.js` (новый)

```javascript
// Автоматически генерируем меню из config.json
async function generateNavigation() {
    const response = await fetch('/data/config.json');
    const config = await response.json();
    
    const nav = document.querySelector('nav ul');
    nav.innerHTML = ''; // Очищаем
    
    // Только маршруты с inMenu: true
    config.routes
        .filter(route => route.inMenu)
        .forEach(route => {
            const li = document.createElement('li');
            const a = document.createElement('a');
            a.href = route.path;
            a.textContent = route.title;
            a.setAttribute('data-link', '');
            li.appendChild(a);
            nav.appendChild(li);
        });
}

// Вызываем при загрузке
window.addEventListener('load', generateNavigation);
```

**Результат:**

```html
<!-- Автоматически сгенерировано из config.json -->
<nav>
    <ul>
        <li><a href="/" data-link>Главная</a></li>
        <li><a href="/about" data-link>О проекте</a></li>
        <li><a href="/technology" data-link>Технологии</a></li>
        <li><a href="/contact" data-link>Контакты</a></li>
    </ul>
</nav>
```

### Автогенерация Sitemap.xml

**Файл:** `build/generate-sitemap.js` (новый)

```javascript
// Node.js скрипт для генерации sitemap.xml из config.json
const fs = require('fs');
const config = require('../data/config.json');

const baseUrl = 'https://el-max-git.github.io/quantum_supremacy';

const sitemap = `<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
${config.routes
    .filter(route => route.inMenu) // Только публичные страницы
    .map(route => `
    <url>
        <loc>${baseUrl}${route.path}</loc>
        <changefreq>weekly</changefreq>
        <priority>${route.path === '/' ? '1.0' : '0.8'}</priority>
    </url>
`).join('')}
</urlset>`;

fs.writeFileSync('sitemap.xml', sitemap);
console.log('✅ sitemap.xml generated from config.json');
```

**Использование:**

```bash
node build/generate-sitemap.js
```

### Метаданные для SEO

Расширим конфигурацию:

```json
{
  "path": "/about",
  "page": "about.html",
  "title": "О проекте - Quantum Supremacy",
  "inMenu": true,
  "meta": {
    "description": "Узнайте больше о нашем проекте по исследованию квантовой физики",
    "keywords": ["quantum", "physics", "research", "about"],
    "ogImage": "/assets/images/about-og.jpg",
    "canonical": "https://el-max-git.github.io/quantum_supremacy/about"
  }
}
```

**Использование в router.js:**

```javascript
async loadRoute(path) {
    // ... загрузка контента ...
    
    // Обновляем метатеги
    const routeConfig = this.getRouteConfig(path);
    if (routeConfig && routeConfig.meta) {
        this.updateMetaTags(routeConfig.meta);
    }
}

updateMetaTags(meta) {
    // Title
    if (meta.title) {
        document.title = meta.title;
    }
    
    // Description
    if (meta.description) {
        document.querySelector('meta[name="description"]')
            .setAttribute('content', meta.description);
    }
    
    // Keywords
    if (meta.keywords) {
        document.querySelector('meta[name="keywords"]')
            .setAttribute('content', meta.keywords.join(', '));
    }
    
    // Open Graph
    if (meta.ogImage) {
        document.querySelector('meta[property="og:image"]')
            .setAttribute('content', meta.ogImage);
    }
}
```

---

## 🔒 Защита routes конфигурации

### robots.txt

```
# Блокируем прямой доступ к data/
User-agent: *
Disallow: /data/
Allow: /
```

### CSP Header (для Apache)

```apache
# .htaccess
<FilesMatch "config\.json$">
    Header set Content-Security-Policy "default-src 'none'"
</FilesMatch>
```

**GitHub Pages:** Конфиг публичный, но это не критично (нет секретов).

---

## 📝 Структура Route объекта

```typescript
interface Route {
    // Обязательные поля
    path: string;           // URL путь (например: "/about")
    page: string;           // Имя файла (например: "about.html")
    title: string;          // Заголовок страницы
    inMenu: boolean;        // Показывать в навигации?
    
    // Опциональные поля
    meta?: {
        description?: string;       // SEO описание
        keywords?: string[];        // SEO ключевые слова
        ogImage?: string;           // Open Graph изображение
        canonical?: string;         // Канонический URL
    };
    
    permissions?: string[];         // ["public", "admin"]
    analytics?: boolean;            // Отслеживать аналитику?
    cache?: boolean;                // Кешировать страницу?
    preload?: boolean;              // Предзагружать?
    icon?: string;                  // Иконка для меню
    order?: number;                 // Порядок в меню
}
```

---

## 🎯 Примеры использования

### Добавить новую страницу "Блог"

**1. Создаем файл:**

```bash
# pages/blog.html
<div class="container">
    <h1>Блог</h1>
    <p>Последние новости о квантовых технологиях</p>
</div>
```

**2. Добавляем в config.json:**

```json
{
  "path": "/blog",
  "page": "blog.html",
  "title": "Блог - Quantum Supremacy",
  "inMenu": true,
  "meta": {
    "description": "Блог о квантовых технологиях и исследованиях",
    "keywords": ["quantum", "blog", "news"]
  },
  "order": 5
}
```

**3. Готово!** Страница автоматически:
- ✅ Доступна по адресу `/blog`
- ✅ Появится в меню навигации
- ✅ Будет работать с роутером
- ✅ Поддержит кнопки назад/вперед

### Скрыть страницу из меню

```json
{
  "path": "/secret",
  "page": "secret.html",
  "title": "Секретная страница",
  "inMenu": false  // ← Не показывать в меню
}
```

Страница доступна по прямой ссылке, но не видна в навигации.

### Добавить внешнюю ссылку

```json
{
  "path": "https://github.com/el-max-git/quantum_supremacy",
  "title": "GitHub",
  "inMenu": true,
  "external": true  // ← Маркер внешней ссылки
}
```

---

## 🚨 Важные замечания

### 1. Асинхронная инициализация

```javascript
// ❌ НЕПРАВИЛЬНО - router.start() до загрузки конфига
const router = new Router();
router.start(); // Нет маршрутов!
initializeRoutes(); // Загрузка маршрутов

// ✅ ПРАВИЛЬНО - router.start() после загрузки
const router = new Router();
await initializeRoutes(); // Сначала загружаем
router.start(); // Потом запускаем
```

### 2. Fallback обязателен

Всегда предусматривайте fallback на случай ошибки загрузки:

```javascript
try {
    await initializeRoutes();
} catch (error) {
    console.error('Config failed, using defaults');
    registerDefaultRoutes(); // ← Обязательно!
}
router.start();
```

### 3. Кеширование config.json

Браузер может кешировать `config.json`. Для принудительного обновления:

```javascript
// Добавляем cache-busting параметр
const timestamp = Date.now();
const response = await fetch(`${basePath}/data/config.json?v=${timestamp}`);
```

Или используйте версионирование:

```javascript
// config.json → config.v2.json
const response = await fetch(`${basePath}/data/config.v2.json`);
```

---

## 📚 Связанные файлы

- `data/config.json` - конфигурация маршрутов
- `index.html` - инициализация динамических маршрутов
- `router.js` - основная логика роутера
- `docs/ROUTER_ARCHITECTURE.md` - архитектура роутера

---

## 💡 Заключение

**Динамические маршруты** превращают роутер из жестко закодированного в гибкую конфигурируемую систему:

- ✅ **Единый источник правды** - `data/config.json`
- ✅ **Простота добавления страниц** - редактируем JSON
- ✅ **Автогенерация компонентов** - меню, sitemap
- ✅ **Расширяемость** - метаданные, SEO, аналитика
- ✅ **Fallback механизм** - сайт работает всегда

**Теперь карта сайта управляется из одного места!** 🚀

---

**Дата создания:** 31.12.2025  
**Версия:** 1.0  
**Автор:** AI Agent (Cursor)
