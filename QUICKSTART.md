# 🚀 Быстрый старт - Quantum Supremacy

## Шаг 1: Инициализация Git репозитория

```bash
# Инициализировать Git (если еще не сделано)
git init

# Добавить все файлы
git add .

# Сделать первый коммит
git commit -m "feat: initial project setup"
```

## Шаг 2: Создание репозитория на GitHub

1. Перейдите на [GitHub](https://github.com/new)
2. Создайте новый репозиторий с именем `quantum_supremacy`
3. **НЕ** добавляйте README, .gitignore или LICENSE (они уже есть)

## Шаг 3: Связывание с GitHub и отправка кода

```bash
# Замените YOUR-USERNAME на ваше имя пользователя GitHub
git remote add origin https://github.com/YOUR-USERNAME/quantum_supremacy.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Отправьте код на GitHub
git push -u origin main
```

## Шаг 4: Настройка GitHub Pages

### Автоматический деплой (рекомендуется)

GitHub Actions уже настроен для автоматического деплоя:

1. Перейдите в `Settings` → `Pages`
2. В разделе "Source" выберите `gh-pages` branch
3. Нажмите `Save`
4. Ваш сайт будет доступен по адресу: `https://YOUR-USERNAME.github.io/quantum_supremacy/`

### Ручной деплой (альтернатива)

1. Перейдите в `Settings` → `Pages`
2. В разделе "Source" выберите `main` branch
3. Выберите папку `/ (root)`
4. Нажмите `Save`

## Шаг 5: Локальная разработка

### Вариант 1: Простой способ
Просто откройте `index.html` в браузере

### Вариант 2: С локальным сервером (рекомендуется)

**Если у вас есть Node.js:**
```bash
# Запустить dev сервер
npm run start
# или
npm run dev
```

**Если у вас есть Python:**
```bash
# Python 3
python -m http.server 8000

# Затем откройте http://localhost:8000
```

**С VS Code/Cursor:**
1. Установите расширение "Live Server"
2. Правый клик на `index.html`
3. Выберите "Open with Live Server"

## Шаг 6: Персонализация

Отредактируйте следующие файлы, заменив placeholder информацию:

1. **README.md**
   - Замените `your-username` на ваше имя пользователя GitHub
   - Обновите имя автора

2. **package.json**
   - Обновите поля `author`, `repository.url`, `bugs.url`, `homepage`

3. **index.html**
   - Настройте мета-теги
   - Измените контент под ваши нужды

4. **SECURITY.md**
   - Добавьте реальный email для сообщений о безопасности

## Следующие шаги

✅ Добавьте реальный контент на страницу  
✅ Добавьте изображения в `assets/images/`  
✅ Создайте favicon и поместите в `assets/icons/`  
✅ Настройте цветовую схему в `css/style.css` (CSS переменные в `:root`)  
✅ Добавьте дополнительные страницы при необходимости  
✅ Пригласите других участников  

## Полезные команды Git

```bash
# Проверить статус
git status

# Добавить изменения
git add .

# Сделать коммит
git commit -m "feat: ваше описание"

# Отправить на GitHub
git push

# Создать новую ветку для функции
git checkout -b feature/new-feature

# Переключиться на main
git checkout main

# Обновить локальную копию
git pull
```

## Получение помощи

- 📖 Читайте [SETUP.md](docs/SETUP.md) для подробных инструкций
- 🤝 Читайте [CONTRIBUTING.md](CONTRIBUTING.md) для информации о вкладе
- 🐛 Создайте [Issue](../../issues) если нашли проблему
- 💬 Задавайте вопросы в [Discussions](../../discussions)

---

**Поздравляем! Ваш проект готов к работе! 🎉**

Не забудьте поставить ⭐ если проект вам понравился!

